# docs agent — 2026:07:10T12:30:08

- **Branch**: agent/docs/2026-07-10
- **Started**: 2026-07-10T12:30:08Z
- **Finished**: 2026-07-10T12:33:00Z
- **Status**: complete

## Summary
- Reviewed the two open `documentation` issues (#635, #624). #635 (JSON parser `required`/`validateSchema` default-on) is already fully addressed on the docs side in `docs/parser/included-parsers.md`, `docs/parser/index.md`, and `docs/examples/concepts/working-with-json.md`; the only remaining items (TSDoc + regression test) are source changes outside docs scope.
- Cross-referenced `src/parser/_functions.ts` parser registry against the parser reference and found a real gap: the `listToKeyValue` parser accepts a `keyTransform` option (`"preserve" | "camelCase"`, default `"preserve"`) that was undocumented. Added an Options table to its section in `docs/parser/included-parsers.md`, matching the style of the other parser sections.
- Regenerated `docs/public/llms-full.txt` with `scripts/generate-llms-txt.sh`. This picked up the new option and also re-synced previously-stale content (the committed file still had `.html` link extensions that were removed from the source docs in an earlier commit, plus several cross-reference links added to source).

## Files Changed
- `docs/parser/included-parsers.md` — documented `listToKeyValue` `keyTransform` option + added a `createParser("listToKeyValue")` snippet and a note on colon-splitting / duplicate-key ordering.
- `docs/public/llms-full.txt` — regenerated from source (includes the new option and re-syncs stale link/xref content).

## Next Steps
- The `docs/public/llms-full.txt` generated file drifts out of sync whenever source docs change without a regeneration step. Consider wiring `scripts/generate-llms-txt.sh` into CI or a pre-commit/build hook so the generated file can't go stale.
- Issue #635 remaining work is source-only: TSDoc on `validateSchema?` in `src/interfaces/parser.ts` and a regression test in `JsonParser.test.ts` — hand off to the coder agent.
- Issue #624 (Pa11y WCAG2AA theme contrast) is a docs-site theme/config change, not content — needs a separate focused run touching `.vitepress` theme styles.
- Spot-check other parser option interfaces (e.g. `ReplaceStringTemplateParser`) for any additional undocumented options.

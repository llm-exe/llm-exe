# docs-sync agent — 2026:06:18T16:52:22

- **Branch**: agent/docs-sync/2026-06-18
- **Started**: 2026-06-18T16:52:22Z
- **Finished**: —
- **Status**: running

## Summary
- Reviewer agent's `--max-turns` was raised from 30 to 60 in `agent-review-pr.yml` (commits `ccbcf05`, `4389b19`). Synced all five references in `AGENT_REVIEW_PR_DEEP_DIVE.md` (job-step diagram, external-calls diagram, tool allowlist code block, state-machine timeout note, failure-mode node, quick-reference card) and the tool-allowlist table in `WORKFLOW_ARCHITECTURE.md`.
- Added a `docs-sync-trigger.yml` row to `WORKFLOWS_INDEX.md` and refreshed the `docs-sync.yml` trigger description to "workflow_dispatch (manual or from docs-sync-trigger)" since push handling is now in the separate trigger workflow.
- Cleaned four pre-existing em-dashes in `WORKFLOW_ARCHITECTURE.md` (rows for `APP_CLIENT_ID`, `APP_BOT_USER_ID`, `LLM_EXE_REVIEW_BOT_CLIENT_ID`, and the replication recipe) since they sit in files we touched.
- The vast majority of recent commits were action-version bumps (`checkout@v6`, `create-github-app-token@v3`, `upload-artifact@v7`, `setup-node@v6`, `cache@v5`) and the `vars.APP_CLIENT_ID` / `vars.APP_BOT_USER_ID` rename. Spot-checked: every deep dive already reflects these versions and var names, so no extra edits were needed.

## Files Changed
- `.github/docs/AGENT_REVIEW_PR_DEEP_DIVE.md` (synced against `.github/workflows/agent-review-pr.yml`)
- `.github/docs/WORKFLOW_ARCHITECTURE.md` (synced against `.github/workflows/agent-review-pr.yml`; also style cleanup)
- `.github/docs/WORKFLOWS_INDEX.md` (synced against `.github/workflows/docs-sync-trigger.yml` and `.github/workflows/docs-sync.yml`)

## Next Steps
- `WORKFLOW_ARCHITECTURE.md` section 9 catalog ends at 9.18 (`test-github-action.yml`). Missing 9.19 `docs-sync-trigger.yml`, 9.20 `docs-sync.yml`, 9.21 `vitals.yml` catalog entries. Add them on a future run when there is more time budget.
- `WORKFLOWS_INDEX.md` header still says "all 16 workflows" but the repo has 21 (`.github/workflows/*.yml`). Tighten that on the next run.
- The "At a glance" tile groups in `WORKFLOWS_INDEX.md` do not list `docs-sync-trigger`, `docs-sync`, or `vitals` deep dives. Either add them or note the omission explicitly.
- Many existing em-dashes remain throughout deep dives outside the files I touched. A focused cleanup run with `grep -rnP '[\x{2014}\x{2013}]' .github/docs/` would knock them out.
- Verify that `DOCS_SYNC_DEEP_DIVE.md` section 6 sequence diagram participant "Event (push or dispatch)" should drop the push half now that push only reaches docs-sync via the trigger workflow.

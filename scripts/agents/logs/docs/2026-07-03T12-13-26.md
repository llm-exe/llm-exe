# docs agent — 2026:07:03T12:13:26

- **Branch**: agent/docs/2026-07-03
- **Started**: 2026-07-03T12:13:26Z
- **Finished**: 2026-07-03T12:16:00Z
- **Status**: complete

## Summary
- Continued work on issue #635 (JSON parser enforces `required` by default). Verified the behavior directly against `src/parser/parsers/JsonParser.ts` (`shouldValidateSchema = !!schema && validateSchema !== false`; missing required fields throw `parser.schema_validation_failed`).
- Closed the two remaining doc gaps flagged in the issue thread: the JSON tips example and the parser landing page never stated the default-on validation behavior.
- Completed the truncated ending of `docs/examples/concepts/working-with-json.md` and added a "What the parser does at runtime" section (required enforced by default, defaults applied after validation, `validateSchema: false` opts out) plus a runnable `defineSchema` + `json` parser example.
- Added a warning callout under "Using a Parser with Schema" in `docs/parser/index.md` linking to the full JSON parser reference.
- Confirmed `docs/parser/included-parsers.md` was already updated (json + listToJson `validateSchema` rows/warnings present); left it unchanged.

## Files Changed
- `docs/examples/concepts/working-with-json.md` — completed truncated content; added runtime-behavior section and typed example.
- `docs/parser/index.md` — added default-on validation warning callout.

## Next Steps
- Source-side follow-ups for #635 are out of docs scope: add TSDoc to `validateSchema?` in `src/interfaces/parser.ts` so the default surfaces on IDE hover, and add the regression test to `JsonParser.test.ts`. These belong to the coder agent — issue #635 can stay open until they land.
- `docs/examples/concepts/working-with-json.md` had several pre-existing typos in prose ("One useful is", "Muse be one of", "over an over"). Left them for a focused copy-edit pass to keep this PR scoped to the #635 fix.
- Consider a broader audit of `docs/examples/` — a few concept pages read as drafts and may have similar truncation/typo issues.

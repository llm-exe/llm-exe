# coder agent — 2026:08:13T09:12:29

- **Branch**: agent/coder/2026-08-13-issue-741
- **Started**: 2026-08-13T09:12:29Z
- **Finished**: 2026-08-13T09:18:00Z
- **Status**: complete

## Summary
- Fixed #741: `LlmFunctionParser` — the class the `@deprecated` notice on `LlmNativeFunctionParser` tells users to migrate to — was not exported, so `import { LlmFunctionParser } from "llm-exe"` did not resolve.
- Exported `LlmFunctionParser` from `src/parser/index.ts` and the package root (`src/index.ts`) alongside the existing deprecated names. Purely additive, no renames or removals.
- Renamed the options interface to the canonical `LlmFunctionParserOptions` and kept `LlmNativeFunctionParserOptions` as a deprecated type alias; both are now exported as types from the parser index and package root (neither was publicly exported before, so nothing breaks).
- Added tests: export-surface assertions in `src/index.test.ts`, plus a block in the parser test that pins the public export to the same class and covers the behavioural difference between the two parsers (normalized `OutputResultContent[]` vs `{ name, arguments }`).
- Verified: `npm test` 198 suites / 3004 tests pass, `npm run typecheck` clean, `npm run lint` unchanged from baseline (130 pre-existing errors, none in touched files).

## Files Changed
- `src/parser/parsers/LlmNativeFunctionParser.ts` — canonical `LlmFunctionParserOptions` + deprecated alias
- `src/parser/index.ts` — export `LlmFunctionParser` and both options types
- `src/index.ts` — re-export `LlmFunctionParser` and both options types from the package root
- `src/parser/parsers/LlmNativeFunctionParser.test.ts` — public-export + behavioural-difference tests
- `src/index.test.ts` — root export surface assertions

## Next Steps
- Docs follow-up (out of scope here, `docs/` is off-limits for this agent): now that `LlmFunctionParser` is exported, `docs/parser/included-parsers.md` and `docs/executor/openai-functions.md` can document it. Open docs PR #742 may be the right place.
- `npm run lint` reports 130 pre-existing errors on `main` (unrelated to this change) — worth a dedicated lint-cleanup pass; noticed but not filed, since it is repo-wide rather than a single defect.
- Known Issues in CLAUDE.md still list a `NumberParser` bug (`'42'` → `'4'`) and `setToolMessage`/`setToolCallMessage` not returning `this`; not investigated this run.

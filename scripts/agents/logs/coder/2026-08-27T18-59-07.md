# coder agent — 2026:08:27T18:59:07

- **Branch**: agent/coder/2026-08-27-issue-761
- **Started**: 2026-08-27T18:59:07Z
- **Finished**: 2026-08-27T19:02:00Z
- **Status**: complete

## Summary
- Worked assigned issue #761 — add a `google.gemini-3.7-flash` shorthand for Google's current top model.
- Added the shorthand to the Google config map via `withDefaultModel(googleGeminiChatV1, "gemini-3.7-flash")`, slotted beside `google.gemini-3.6-flash`.
- Added the matching `Omit<GeminiRequest, "model">` entry to the shorthand map in `src/interfaces/llm.ts` so type inference picks it up.
- Added test coverage mirroring the existing `gemini-3.6-flash` block (base config inheritance + model override in `mapBody`/`options`).
- Verified: `npm test` 199 suites / 3078 tests passing, `npm run typecheck` clean, `eslint` clean on all changed files.
- Purely additive — no behavior change to any existing shorthand.

## Files Changed
- `src/llm/config/google/index.ts` — added `google.gemini-3.7-flash` shorthand
- `src/interfaces/llm.ts` — added matching shorthand-map entry for type inference
- `src/llm/config/google/google.test.ts` — added `gemini-3.7-flash` describe block

## Next Steps
- **Preview models intentionally skipped.** Issue #761 flagged `gemini-3.1-pro-preview` and `gemini-3-flash-preview` as present in Google's list but recommended leaving them alone — preview names churn. No action needed unless they graduate to stable.
- **`effort` mapping is stale and only covers 2.5 models.** In `src/llm/config/google/index.ts`, the `mapBody.effort` transform gates the thinking-budget on a hardcoded `["gemini-2.5-pro", "gemini-2.5-flash", "gemini-2.5-flash-lite"]` list — all three of which are now *deprecated* shorthands in this same file. That means `effort` is silently a no-op for every current 3.x shorthand (3.1-flash-lite, 3.5-flash, 3.5-flash-lite, 3.6-flash, and the new 3.7-flash). Worth a separate issue: decide whether 3.x supports thinking budgets and either extend the list or drop the option. Out of scope here — did not touch it, per one-issue-per-run.
- **Repo-wide lint debt.** `npm run lint` reports 131 pre-existing problems (130 errors) across `src/utils/modules/*` and elsewhere — e.g. `no-empty` in `json.ts:8`, `no-prototype-builtins` in `pick.ts:6`. None are from this change (eslint is clean on all files touched here), but the lint gate is effectively unusable as a signal until that backlog is cleared.

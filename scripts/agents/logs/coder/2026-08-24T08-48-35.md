# coder agent — 2026:08:24T08:48:35

- **Branch**: agent/coder/2026-08-24-issue-757
- **Started**: 2026-08-24T08:48:35Z
- **Finished**: 2026-08-24T08:55:00Z
- **Status**: complete (partial scope — see Next Steps)

## Summary

- Worked issue #757 (support content blocks / images in tool-function results). Scoped this run to the path that makes the Anthropic acceptance criterion work end-to-end; the remaining provider work is independently landable on top.
- Widened `IChatFunctionMessage.content` to `string | IChatMessageContentDetailed[]` (the loose union, matching what user messages already accept — per the research comment on the issue, using the strict union would have traded one asymmetry for another). Widened `ChatPrompt.addFunctionMessage`, the `addToPrompt("function", ...)` overload, and `Dialogue.setFunctionMessage` / `setToolMessage` to match.
- Fixed the real blocker: `ChatPrompt.format()` and `formatAsync()` templated function-message content unconditionally, so array content threw a raw Handlebars error (`You must pass a string or Handlebars AST`) before any provider config ran. Both branches now mirror the user-message branch — template `text` blocks, pass image blocks through.
- Fixed the Anthropic mapper: image blocks were already being converted to Anthropic `image`/`source` shape at the top of the callback, then `maybeStringifyJSON` flattened the whole array into a JSON string on the `role === "function"` branch — base64 billed as text tokens, model never sees the image. It now emits the converted array as `tool_result.content`; `maybeStringifyJSON` stays on the non-array path.
- Verification: full suite green (198 suites / 3070 tests), `tsc --noEmit` clean, lint diff-clean (131 problems are all pre-existing — confirmed by stashing and re-running).

## Files Changed

- `src/interfaces/chat.ts` — widened `IChatFunctionMessage.content`
- `src/prompt/chat.ts` — `addFunctionMessage` / `addToPrompt` signatures; `format()` and `formatAsync()` function-message branches
- `src/prompt/chat.test.ts` — new `function messages with content blocks` describe: sync + async templating, string-content regression, `addFromHistory` round trip
- `src/state/dialogue.ts` — `setFunctionMessage` / `setToolMessage` signatures
- `src/llm/config/anthropic/promptSanitizeMessageCallback.ts` — pass block arrays through as `tool_result.content`
- `src/llm/config/anthropic/promptSanitizeMessageCallback.test.ts` — native image tool_result, url-source conversion, url-source rejection when `allowImageUrlSources: false`
- `scripts/agents/logs/coder/2026-08-24T08-48-35.md` — this log

## Next Steps

Remaining #757 items, in the order I'd take them (issue stays open — the PR says "Part of", not "Fixes"):

1. **Google mapper.** The block→part conversion is gated behind `message.role !== "function"` in `src/llm/config/google/promptSanitizeMessageCallback.ts` (~line 70), so array content lands raw inside `functionResponse.response.result`. `googleGeminiContentBlockToPart` already exists and does the right thing — it just isn't reachable from the function branch. Pin the exact multimodal `functionResponse` shape (`parts` vs nested under `response`) against live Gemini docs first; it varies by API version and can't be verified from the repo.
2. **Typed throws for text-only providers.** With the type widened, OpenAI chat-completions / xAI / Deepseek now pass array content straight onto `role: "tool"` and get a provider 400 instead of an `LlmExeError`. Same for Bedrock invoke. Should throw `prompt.invalid_messages` with a resolution message, matching how the Bedrock config rejects URL image sources today.
3. **Ollama role guard.** `ollamaPromptSanitize` (`src/llm/config/ollama/promptSanitize.ts:20`) branches on `Array.isArray(content)` with no role check, so an image tool result silently becomes a `function` message with joined text plus a top-level `images: []` — a field Ollama's tool messages don't support. Silently wrong rather than erroring. Won't inherit a guard from item 2; it doesn't share the OpenAI callback.
4. **Contract fixture.** `src/llm/config/promptSanitizeContract.test.ts` is registry-driven off `configs`; adding one `"image tool result"` fixture next to the existing `"tool result"` one forces every provider to declare pass-through-or-throw. The `{ throws: string }` expectation variant already supports the error cases. Best done after 2 and 3 so it locks in the declared behavior.
5. **Out of scope for #757 but unblocked by it:** an `openai.responses.v1` provider config — the Responses API is where OpenAI actually ships image function-call outputs. Separate, larger decision.

Noted but not touched (no new issues filed): nothing outside #757 came up this run.

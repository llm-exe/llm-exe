# coder agent — 2026:08:31T16:01:17

- **Branch**: agent/coder/2026-08-31-issue-732
- **Started**: 2026-08-31T16:01:17Z
- **Finished**: 2026-08-31T16:05:00Z
- **Status**: complete

## Summary

- Fixed issue #732: `xai.grok-4.20-reasoning` silently dropped the `effort` option because `isReasoningModel` in `src/llm/config/x/index.ts` only matched `grok-4.3` / `grok-4.5` / `grok-4.6`, not the shorthand's resolved model `grok-4.20-0309-reasoning`.
- Replaced the inline equality chain with an explicit `XAI_REASONING_MODELS` list including `grok-4.20-0309-reasoning`. Used an explicit list rather than a `endsWith("-reasoning")` suffix match, since `grok-4.20-0309-non-reasoning` also ends in `-reasoning`.
- Removed `grok-4.20-0309-reasoning` from the "drops effort for non-reasoning xAI models" test case, where it had been grouped by mistake.
- Added tests: valid effort values pass through for `grok-4.20-0309-reasoning`, invalid values (`none`, `xhigh`, `123`) are still dropped, and `grok-4.20-0309-non-reasoning` still drops effort. Added an end-to-end assertion through the `xai.grok-4.20-reasoning` shorthand config.
- Verified: `npm test` 199 suites / 3144 tests pass, `npm run typecheck` clean, `npx eslint src/llm/config/x/` clean.

## Files Changed

- `src/llm/config/x/index.ts` — added `XAI_REASONING_MODELS` list, `isReasoningModel` now checks membership
- `src/llm/config/x/xai.test.ts` — moved `grok-4.20-0309-reasoning` out of the drop list, added 3 tests

## Next Steps

- **Docs follow-up (out of scope for this agent).** `docs/llm/xai.md` documents the old behavior — that `effort` only reaches `grok-4.3` / `grok-4.5`. It needs updating to include `grok-4.6` and `grok-4.20-0309-reasoning` once this PR merges. Noted on issue #732 by the reporter.
- **Pre-existing lint debt.** `npm run lint` reports 130 errors / 1 warning repo-wide, none in the files touched here. Includes `no-prototype-builtins` violations. Worth a dedicated cleanup issue/PR — not attempted here to keep this PR focused.
- **`isReasoningModel` duplication.** Each provider config hand-maintains its own reasoning-model predicate. As model lists grow, a shared helper (or per-provider model metadata table) may be worth considering, but it's speculative right now — not a change to make without a concrete second motivating case.
- **CLAUDE.md Known Issues still open**: NumberParser `'42'` → `'4'`, and `setToolMessage`/`setToolCallMessage` not returning `this`. Neither had a labeled issue matched this run; good candidates for the next run.

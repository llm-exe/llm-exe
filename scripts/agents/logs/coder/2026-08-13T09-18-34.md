# coder agent — 2026:08:13T09:18:34

- **Branch**: agent/coder/2026-08-13-issue-736
- **Started**: 2026-08-13T09:18:34Z
- **Finished**: 2026-08-13T09:21:00Z
- **Status**: complete — PR #744 (draft, base `development`)

## Summary
- Worked assigned issue #736: `google.gemini-1.5-pro` shorthand points at a model Google has fully retired (absent from both the models list and the deprecations page), so it 404s for anyone who reaches for it.
- Wrapped the shorthand in the existing `deprecateShorthand` helper rather than deleting the key — removing an exported shorthand would be a breaking change, and the issue is milestoned v3.1.0. The shorthand still resolves; using it now emits a `DeprecationWarning`.
- The issue's suggested replacement (`gemini-2.5-pro`) is itself already deprecated in this repo, so the warning message points at the current stable shorthands: `google.gemini-3.5-flash` / `google.gemini-3.1-flash-lite`. Called this out on the issue and in the PR body.
- Added a `deprecated shorthands still resolve` test block covering all four deprecated Gemini shorthands (resolved model, inherited base config, `deprecated` metadata) plus a targeted assertion on the 1.5-pro message. These fail without the fix — `cfg.deprecated` was `undefined`.
- Verified: `npm test` 198 suites / 3006 tests pass, `npm run typecheck` clean, `npm run lint` shows no new errors in the changed files.

## Files Changed
- `src/llm/config/google/index.ts` — modified
- `src/llm/config/google/google.test.ts` — modified

## Next Steps
- **#231 — Gemini 2.0 shutdown.** Deliberately left out of this PR to keep it to one concern, but `google.gemini-2.0-flash` and `google.gemini-2.0-flash-lite` in `src/llm/config/google/index.ts` are still plain non-deprecated shorthands past their shutdown date. Same one-line `deprecateShorthand` treatment applies; would be a natural next run.
- **Deprecated 2.5-series shutdown dates have passed.** `google.gemini-2.5-flash` / `-2.5-pro` carry "will shut down on 2026-06-17" and `-2.5-flash-lite` "2026-07-22" — all in the past as of today. The messages read as future-tense warnings for models that are already gone; worth a sweep to reword to past tense across providers, not just Google.
- **Pre-existing lint debt.** `npm run lint` reports 131 problems repo-wide (130 errors), including `'fail' is not defined` (`no-undef`) in `src/llm/config/google/promptSanitize.test.ts:43,61` and `no-prototype-builtins`. Untouched here — out of scope for a single-issue run, but it makes lint useless as a PR gate until cleaned up or the config is adjusted.

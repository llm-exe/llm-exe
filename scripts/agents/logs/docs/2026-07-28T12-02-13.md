# docs agent — 2026:07:28T12:02:13

- **Branch**: agent/docs/2026-07-28
- **Started**: 2026-07-28T12:02:13Z
- **Finished**: 2026-07-28T12:10:00Z
- **Status**: complete

## Summary
- No open `documentation`-labeled issues this run, so I audited the public export surface in `src/index.ts` against the docs to find real gaps.
- Followed up on the previous run's suggestion to spot-check parser option interfaces: `ListToJsonParser` (`keyTransform`, `schema`, `validateSchema`), `JsonParser` (`match`, `schema`, `validateSchema`), `BooleanParser`, `NumberParser`, and `StringExtractParser` options are all documented accurately in `docs/parser/included-parsers.md`. `ReplaceStringTemplateParser`, `MarkdownCodeBlock(s)`, `ListToArrayParser`, and `StringParser` take no constructor options — nothing missing. No changes needed there.
- Found and filled a real gap on the tool-calling page: `docs/executor/openai-functions.md` showed how to define tools but never documented what `execute()`'s second argument accepts or what the executor actually returns.
- Added an execute-options table (`functions`, `functionCall`, `functionCallStrictInput`, `jsonSchema`), a "Handling the Response" section covering the two return shapes (`OutputResultContent[]` with `type: "function_use"` when tools are called vs. parser output on the text path) using the exported `guards.isFunctionCall` type guard, a warning that a custom parser only applies to the text path, a note on replying with `functionId`, and See Also cross-links.
- Regenerated `docs/public/llms-full.txt`; `npm run typecheck` passes.

## Files Changed
- `docs/executor/openai-functions.md` — documented execute options, response shapes, `guards.isFunctionCall` usage, parser-scope warning, and cross-links.
- `docs/public/llms-full.txt` — regenerated (only picks up the new content; the file was otherwise in sync).

## Next Steps
- `LlmNativeFunctionParser` is exported from `src/index.ts` but is marked `@deprecated` in source (superseded by `LlmFunctionParser`, which is **not** exported from the package root). It has zero mentions in `docs/`. Two things for a future run: (a) decide whether `LlmFunctionParser` should be exported publicly — that's a source change, so file an issue rather than fixing it; (b) once settled, document the parser (or explicitly document `LlmNativeFunctionParser`/`OpenAiFunctionParser` as deprecated-only exports in a migration note).
- The "Replying With Tool Results" section I added is prose-only. A working round-trip example (call tool → append result with `functionId` → re-execute) would be the single most useful addition to the tool-calling docs. `examples/basicTools.ts` only covers the first half of the loop.
- Still worth wiring `scripts/generate-llms-txt.sh` into CI or a pre-commit hook — the generated file drifts whenever docs change without a manual regeneration (carried over from the 2026-07-10 run).
- Issue #624 (Pa11y WCAG2AA theme contrast) remains open and untouched — it's a `.vitepress` theme/config change, not content, and needs its own focused run.

# coder agent — 2026:08:20T08:42:36

- **Branch**: agent/coder/2026-08-20-issue-736
- **Started**: 2026-08-20T08:42:36Z
- **Finished**: 2026-08-20T08:45:00Z
- **Status**: complete — no code change required

## Summary

- Assigned issue #736 (`google.gemini-1.5-pro` points at a retired model) turned out to be **already fixed** on `development`. No PR was opened.
- The shorthand was swept up alongside the 2.0 shutdown work in commit `07a8a03` ("fix: mark retired Google gemini shorthands as deprecated (closes #231)"), merged via PR #700 — exactly the "might be worth doing both in one PR" outcome #736 itself suggested.
- Verified current state: `src/llm/config/google/index.ts` wraps the key in `deprecateShorthand()` with the 2025-09-29 shutdown date and a `google.gemini-3.5-flash` migration target. The key still resolves (removing it would change `AllUseLlmOptions` — breaking, parked for v4.0.0).
- Verified `07a8a03` is an ancestor of `origin/development`, and ran `npx jest src/llm/config/google` — 66 tests passing across 3 suites, including the `retired shorthands` block at `src/llm/config/google/google.test.ts:298` that asserts the deprecation metadata for `gemini-1.5-pro`.
- Commented on #736 with the evidence and closed it as completed.

## Files Changed

- `scripts/agents/logs/coder/2026-08-20T08-42-36.md` (this log) — no source files touched.

## Next Steps

- **Deviation worth flagging:** #736 suggested migrating users to `gemini-2.5-pro`, but the shipped message points at `google.gemini-3.5-flash` instead. That was deliberate — `google.gemini-2.5-pro` is itself already deprecated with a 2026-06-17 shutdown, so it would move users one step down the same path. Noted in the closing comment.
- **Stale-issue signal:** #736 was filed by the 2026-08-10 scout run against code that PR #700 had already fixed on 2026-07-30. The scout appears to be reading model shorthands without checking whether they are wrapped in `deprecateShorthand`. Worth teaching the scout to treat a `deprecateShorthand`-wrapped key as already handled, otherwise it will keep re-filing the 2.0/2.5 entries too.
- **Pending cleanup for v4.0.0:** the five retired Google shorthands (`gemini-2.0-flash`, `gemini-2.0-flash-lite`, `gemini-1.5-pro`, plus the 2.5 series once their shutdown dates pass) are all deprecation-only placeholders now. Removing them and their `src/interfaces/llm.ts` type entries is a single breaking sweep that should be filed against the next major milestone.
- No open issues matched a `gemini` search at the end of this run, so the Google shorthand backlog is currently clear.
- Known Issues in CLAUDE.md still list the worker-process force-exit warning (`asyncCallWithTimeout.test.ts` timer leak) — untouched, still available for a future run.

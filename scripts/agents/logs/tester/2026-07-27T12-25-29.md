# tester agent — 2026:07:27T12:25:29

- **Branch**: agent/tester/2026-07-27
- **Started**: 2026-07-27T12:25:29Z
- **Finished**: 2026-07-27T12:35:00Z
- **Status**: complete

## Summary

- Baseline coverage is already very high (99.84% stmts / 99.27% branches / 100% lines across 192 suites, 2625 tests), so raw line coverage was not the opportunity. Instead I targeted the specific uncovered branches that carry real risk — secret redaction and error serialization.
- Added `serialize.ts` tests for `Response`-shaped values (fetch failures must be summarized, not stream-dumped) and for cross-copy `LlmExeError` detection via the registered `Symbol.for("llm-exe.error")` tag — the path that keeps serialization working when two copies of llm-exe are installed.
- Added `_utils.parseHeaders.ts` tests proving the malformed-headers error path never leaks credentials: the post-replacement string is both redacted (`sk-ant-…` never reaches message or context) and truncated at the 500-char excerpt cap.
- **Found a bug**: `anthropicPromptSanitize([])` throws `TypeError: Cannot read properties of undefined (reading 'role')` — `promptSanitize.ts:93` destructures `[first, ...messages]` with no empty-input guard, and line 99 dereferences `first`. Filed as [#694](https://github.com/llm-exe/llm-exe/issues/694) (`bug`, not `breaking` — the path crashes today, so no working consumer depends on it). Documented with an `it.failing` test so the bug is pinned without reddening CI; flip it to `it` when the fix lands.
- Full suite and typecheck pass.

## Files Changed

- `src/errors/serialize.test.ts` — Response-like value summarization; symbol-tagged LlmExeError detection (top-level and as a cause)
- `src/llm/_utils.parseHeaders.test.ts` — redaction and truncation of the post-replacement header excerpt in the parse-failure error
- `src/llm/config/anthropic/promptSanitize.test.ts` — `it.failing` test pinning the empty-messages crash (#694)

## Next Steps

- **#694 is for the coder agent.** When the empty-array guard lands in `promptSanitize.ts`, change the `it.failing` back to `it` in `promptSanitize.test.ts`.
- Remaining uncovered branches, all small and low-risk, for a future run:
  - `src/parser/parsers/JsonParser.ts:48` — `findBalancedJsonEnd` entered with a start char that is neither `{` nor `[` (empty stack path).
  - `src/utils/modules/request.ts:117` — non-string `detail` coerced via `JSON.stringify` in the HTTP error message.
  - `src/llm/config/anthropic/index.ts:112` — adaptive-effort model prefix matching (`claude-opus-4-6/4-7/4-8`, `claude-sonnet-4-6`).
  - `src/prompt/_templateValidation.ts:309`.
  - `src/errors/index.ts` and `src/utils/modules/index.ts` — uncovered re-export arrow functions; barrel-file noise, probably not worth chasing.
- `src/utils/modules/redactSecrets.ts:26` (`maskToken`'s `length <= 8` early return) looks **unreachable**: every `TOKEN_MASK_REGEX` alternative matches at least 12 characters (`Bearer a.b.c` is the shortest). It is harmless defensive code — I did not file an issue, but a future run could either drop the branch or ignore it for coverage rather than write a test that cannot be written.
- Beyond branch coverage, the higher-value direction now is behavioral: multi-turn executor composition, provider response normalization under malformed/partial payloads, and end-to-end type-inference assertions (parser output → executor return type), none of which coverage percentages measure.

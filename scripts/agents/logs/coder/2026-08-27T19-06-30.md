# coder agent — 2026:08:27T19:06:30

- **Branch**: agent/coder/2026-08-27-issue-732
- **Started**: 2026-08-27T19:06:30Z
- **Finished**: 2026-08-27T19:10:00Z
- **Status**: complete — PR #769 (draft, base `development`)

## Summary

- Fixed issue #732: `useLlm("xai.grok-4.20-reasoning", { effort: "high" })` silently dropped the effort option.
- Root cause — the shorthand resolves to `grok-4.20-0309-reasoning`, which `isReasoningModel` in `src/llm/config/x/index.ts` did not match, so the shared OpenAI-compatible `effort` transform returned `undefined` and `reasoning_effort` never reached the request body.
- Replaced the chained `===` checks with an explicit `Set` of reasoning model ids including `grok-4.20-0309-reasoning`. Exact matching is deliberate: `grok-4.20-0309-non-reasoning` also ends in `-reasoning`, so a suffix check would wrongly enable effort for it.
- Corrected the test that locked in the bug (`grok-4.20-0309-reasoning` was listed under "drops effort for non-reasoning xAI models") and added pass-through / invalid-value coverage for that model.
- Verified: `npm test` 199 suites / 3078 tests passed, `npm run typecheck` clean, `npm run lint` no findings on changed files (repo has 130 pre-existing errors elsewhere).

## Files Changed

- `src/llm/config/x/index.ts` — reasoning models as an explicit `Set`, now including `grok-4.20-0309-reasoning`.
- `src/llm/config/x/xai.test.ts` — moved the model out of the drop list; added effort pass-through and invalid-value tests.

## Next Steps

- **Docs follow-up (out of scope here)**: `docs/llm/xai.md` documents the old behavior — that effort only reaches `grok-4.3` / `grok-4.5`. It needs updating once #769 merges. Noted in the PR body for the maintainer.
- **Possible related gap, not filed**: xAI also ships `grok-4-fast-reasoning` and `grok-4-1-fast-reasoning` variants (only the `-non-reasoning` ones have shorthands). A user passing those via `useLlm("xai.chat.v1", { model: "grok-4-fast-reasoning", effort: "high" })` would hit the same silent drop. I did not add them because I could not verify the exact model id strings against xAI's API. Worth confirming and filing if real.
- **Pre-existing lint debt**: `npm run lint` reports 130 errors across the repo (e.g. `no-empty` in `src/utils/modules/json.ts:8`, `no-prototype-builtins` in `src/utils/modules/pick.ts:6`). Untouched by this run; may deserve its own cleanup pass or an eslint config decision.

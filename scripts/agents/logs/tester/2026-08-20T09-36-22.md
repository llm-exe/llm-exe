# tester agent — 2026:08:20T09:36:22

- **Branch**: agent/tester/2026-08-20
- **Started**: 2026-08-20T09:36:22Z
- **Finished**: 2026-08-20T09:41:00Z
- **Status**: complete

## Summary

- Ran the suite with coverage and found the codebase already at **99.84% statements / 99.39% branches / 100% lines** (3057 tests, 198 suites). Line coverage is not the opportunity here — the remaining gaps are a handful of specific uncovered branches, so I went after those directly rather than padding the suite.
- Mapped every uncovered branch/statement to a source line via `coverage-final.json` and triaged which are real behavior vs. unreachable defensive guards.
- Added 4 tests for `src/errors/serialize.ts` covering `fetch` `Response` handling — the whole `isResponseLike` path was completely untested. Covers a `Response` in `context`, as a `cause`, and passed directly to the serializer, plus the `statusText`/`url` `?? ""` fallbacks. Also asserts the response body is **never drained** during serialization (`bodyUsed === false`), which matters because a `Response` is a common `cause` on provider failures and its body is single-use.
- Added 2 tests for `src/utils/modules/request.ts` covering the empty-body HTTP error message path: falls back to `statusText`, then to `"Unknown error."` when both body and `statusText` are empty (the real-world HTTP/2 case, where responses carry no reason phrase).
- Full suite: **3063 passed, 198 suites**. `npm run typecheck` clean. No source files touched; no bugs found worth filing.

## Files Changed

- `src/errors/serialize.test.ts` — added `describe("fetch Response values")` block (4 tests)
- `src/utils/modules/request.test.ts` — added `describe("error message fallbacks when the body is empty")` block (2 tests)

## Next Steps

Coverage is high enough that the next runs should focus on **semantic** depth, not the coverage number. Specific leads:

1. **Unreachable code worth flagging to the coder agent** (I did not file issues — these are cosmetic, not bugs, but a reviewer may want them removed or the guards justified):
   - `src/utils/modules/redactSecrets.ts:26` — `maskToken`'s `if (match.length <= 8) return match;` is **dead**. The shortest alternative in `TOKEN_MASK_REGEX` is `AKIA[A-Z0-9]{16}` (20 chars), so `match.length` is always ≥ 20. Untestable through the public API.
   - `src/llm/config/anthropic/promptSanitize.ts:37` — `warnIfTrailingAssistantMessage`'s `if (messages.length === 0) return;` guard is not reached; the empty-array case short-circuits earlier in `anthropicPromptSanitize`. Empty-array behavior itself is already tested.
   - `src/llm/_utils.parseHeaders.ts:11` — `safeReplacedHeaders`'s `if (!value) return "";` falsy branch is uncovered; needs a config whose header template renders to an empty string.
   - `src/parser/parsers/JsonParser.ts:48` — the `: []` fallback in `findBalancedJsonEnd` (start char is neither `{` nor `[`). Check whether any caller can actually reach it before writing a test for it.

2. **Files whose test is much thinner than the source** — likely under-tested behaviorally even at high line coverage:
   - `src/utils/modules/handlebars/helpers/async/each.ts` (140 src / 48 test lines) — the widest gap in the repo. Async `each` over promises, nested contexts, `@index`/`@first`/`@last`, and rejection handling are all worth probing.
   - `src/utils/modules/handlebars/utils/makeHandlebarsInstanceAsync.ts` (155 / 88)
   - `src/prompt/_base.ts` (324 / 282)
   - `src/executor/_functions.ts` (80 / 56)

3. **Type-level coverage is untested anywhere.** The CLAUDE.md non-negotiable is end-to-end inference (prompt input → parser output → executor return). Nothing in the suite asserts that inference still holds. Consider `expectTypeOf`/`tsd`-style compile-time assertions, or `// @ts-expect-error` negative tests, so a regression in executor generics fails CI instead of silently degrading to `any`.

4. **Known issue still open** (from CLAUDE.md, not addressed this run): worker process force-exit warning, suspected timer leak in `asyncCallWithTimeout.test.ts`. Did not reproduce it this run — the suite exited cleanly at 6.3s — but it may be load-dependent.

# docs agent — 2026:09:08T14:20:04

- **Branch**: agent/docs/2026-09-08
- **Started**: 2026-09-08T14:20:04Z
- **Finished**: 2026-09-08T14:25:00Z
- **Status**: complete

## Summary

- Re-checked assigned issue #755 (xAI `effort` on `xai.grok-4.20-reasoning`). **Still blocked** — PR #754 is closed-unmerged and PR #776 is open, so `isReasoningModel` in `src/llm/config/x/index.ts:11-12` still matches only `grok-4.3`, `grok-4.5`, `grok-4.6`. Made no change to the `effort` row; commented on the issue with the current state.
- Fixed deprecation drift in `docs/llm/xai.md`: six shorthands (`xai.grok-2`, `xai.grok-3`, `xai.grok-3-mini`, `xai.grok-4`, `xai.grok-4-fast`, `xai.grok-4-1-fast`) are marked deprecated in source and emit runtime warnings, but were listed as ordinary options; two were used in the usage examples. Split into active/deprecated tables with a reason per row, moved examples to `grok-4.3` / `grok-4.6`.
- Rewrote the shorthand coverage in `docs/llm/gemini.md`, which had **no** shorthand table at all and used the deprecated `google.gemini-2.5-flash` in both examples. Added active (3.x) and deprecated (2.x/1.5) tables and switched examples to `gemini-3.5-flash`.
- Documented the Gemini `effort` gap: the model allowlist covers only the three deprecated 2.5 models, so `effort` is silently dropped on every active 3.x shorthand. Added a warning callout linking [#781](https://github.com/llm-exe/llm-exe/issues/781) (verified open, and it describes exactly this).
- Regenerated `docs/public/llms-full.txt` via `scripts/generate-llms-txt.sh`; diff contains only the two doc edits. `npm run typecheck` passes (exit 0).

## Files Changed

- `docs/llm/xai.md` — split active vs. deprecated shorthand tables; examples moved off deprecated models; frontmatter description updated
- `docs/llm/gemini.md` — added shorthand tables; examples moved off deprecated models; `effort` row clarified plus a warning callout for #781
- `docs/public/llms-full.txt` — regenerated (derived artifact)
- `scripts/agents/logs/docs/2026-09-08T14-20-04.md` — this log

## Next Steps

- **Same deprecation drift remains in `docs/llm/anthropic.md` and `docs/llm/openai.md`.** Neither file mentions deprecation anywhere, but source marks these as deprecated: `anthropic.claude-3-5-haiku`, `anthropic.claude-3-5-sonnet`, `anthropic.claude-3-7-sonnet`, `anthropic.claude-3-opus`, `anthropic.claude-opus-4-6`, `openai.gpt-4`, `openai.gpt-4.1-nano`, `openai.gpt-5-mini`, `openai.gpt-5-nano`, `openai.o3`, `openai.o4-mini`. Apply the same active/deprecated table treatment used here. This is the highest-value next task.
- `examples/helloWorld.test.ts`, `examples/basicTools.test.ts`, and `examples/claude.md` reference deprecated model shorthands. The `.ts` test files are outside the docs agent's scope, but `examples/claude.md` is in scope and should be updated; the tests are worth an issue for the coder/tester agent.
- Issue #755 stays open and blocked on #732 / #776. Once a fix merges, remove the warning callout in `docs/llm/xai.md` and add `grok-4.20-0309-reasoning` to the `effort` model list.
- Likewise, once #781 / PR #783 merges, update the Gemini `effort` row and remove that warning callout.
- Consider a small recurring check: diff `deprecateShorthand(...)` entries in `src/llm/config/**` against the provider docs tables. This drift has recurred across several runs and is mechanical to detect.
- `docs/.vitepress/utils/providers.json` still carries deprecated entries (e.g. `xai.grok-2`); it is generated by `npm run docs:update-providers`. Worth confirming whether it should flag deprecation status so the `<ImportModelNames />` component does not present retired models as current.

# docs agent — 2026:08:25T10:39:20

- **Branch**: agent/docs/2026-08-25
- **Started**: 2026-08-25T10:39:20Z
- **Finished**: 2026-08-25T10:43:00Z
- **Status**: complete

## Summary

- **Worked assigned issue #755 — and found its premise was wrong.** The issue
  asked to document `xai.grok-4.20-reasoning` as supporting `effort` "once
  PR #754 merges". **PR #754 was closed without merging**, and #732 is still
  open. `isReasoningModel` (`src/llm/config/x/index.ts:10-11`) still matches
  only `grok-4.3`, `grok-4.5`, `grok-4.6`. Making the requested edit would have
  documented behavior that does not exist. Commented on #755 with the finding
  and left it open, blocked on #732.
- **Found the real gap instead: `xai.grok-4.6` was undocumented.** The shorthand
  shipped in `fa8e8e7` (closes #749) but never made it into the table in
  `docs/llm/xai.md`, nor into the `effort` row's list of reasoning models. Added
  both.
- **Documented the silent `effort` drop** on `xai.grok-4.20-reasoning` as a
  warning block linking to #732. This is the actual user-facing trap behind
  #755 — the shorthand is *named* `-reasoning` but the parameter is dropped with
  no error — and it is worth documenting while the fix is pending, rather than
  waiting for a PR that got closed.
- **Cleared a carried-over Next Step:** `docs/llm/bedrock/meta.md` had a bare
  "The Bedrock model id" row. Added the three accepted identifier forms matching
  the note already on the Anthropic Bedrock page, and stated explicitly that no
  Meta option is gated on the model name, so the Anthropic page's `effort` ARN
  caveat does *not* apply here.
- Verified `encodeKeys: ["model"]` lives in `src/llm/llm.call.ts:46` (shared by
  all configs), so the URL-encoding claim genuinely covers Meta too — not
  assumed from the Anthropic page. `npm run typecheck` passes; regenerated
  `docs/public/llms.txt` and `llms-full.txt`.

## Files Changed

- `docs/llm/xai.md` (grok-4.6 shorthand row, effort model list, #732 warning)
- `docs/llm/bedrock/meta.md` (model identifier forms note)
- `docs/public/llms.txt`, `docs/public/llms-full.txt` (regenerated)

## Next Steps

- **When #732 is fixed, revisit `docs/llm/xai.md`.** Remove the warning block
  and add `grok-4.20-0309-reasoning` to the `effort` model list. Issue #755
  tracks exactly this and is still open.
- **Shorthand tables drift silently.** `xai.grok-4.6` sat undocumented for a
  while, and this class of miss will recur every time a shorthand ships. The
  `<ImportModelNames />` component already renders the live list from source —
  the hand-maintained markdown tables above it are the thing that goes stale.
  Worth considering whether those tables should be generated too; that is a
  docs-tooling change, so I left it as a recommendation.
- **Audit other providers' shorthand tables** against
  `src/llm/config/*/index.ts` for the same drift. I only checked xAI this run.
  `openai`, `anthropic`, and `gemini` are the likely candidates.
- **Stale source comment** (carried over, still true):
  `src/llm/config/anthropic/effort.ts:49-52` claims llm-exe "cannot yet dispatch
  an ARN model id", which #722 fixed. Contradicts the code. Comment-only, so no
  issue filed — a one-liner for the coder agent.
- **Error docs depth** (carried over, still open): `docs/misc/errors.md` lists
  categories but not the full code table. A code→category→retryable table would
  make the page self-contained.
- **Verify the SigV4 signed path** (carried over): #722 flagged that
  `getAwsAuthorizationHeaders` signs over `url.pathname`, which must match the
  sent path after encoding. Still unconfirmed; needs live Bedrock verification,
  not a docs read.

# coder agent — 2026:06:25T10:43:05

- **Branch**: agent/coder/2026-06-25-issue-630
- **Started**: 2026-06-25T10:43:05Z
- **Finished**: 2026-06-25T10:47:00Z
- **Status**: completed

## Summary
- Fixed issue #630: `CoreExecutor` now forwards the `ExecutionContext` snapshot to user-supplied handlers (previously dropped).
- Extended `CoreExecutorInput.handler` and `createCoreExecutor` signatures to expose an optional `ExecutionContext<I, O>` second arg, matching how `createCustomParser` already exposes context.
- Additive change — handlers that ignore the second argument continue to work unchanged.
- Added 4 new tests covering context delivery (executor metadata, traceId resolution, traceId undefined, backward-compat).
- Updated 1 existing test that asserted handler-call args (now also receives context).

## Files Changed
- `src/interfaces/functions.ts` — handler type accepts optional `ExecutionContext<I, O>`
- `src/executor/core.ts` — forward `_context` through `_handler.call`
- `src/executor/_functions.ts` — `createCoreExecutor` handler signature mirrors the interface
- `src/executor/core.test.ts` — added `execution context` describe block, updated existing call-arg test

## Next Steps
- Docs follow-up (out of scope here, blocked by `docs/` boundary): `docs/executor/execution-context.md` should be updated with a worked CoreExecutor handler example now that the source supports it.
- Pre-existing lint errors remain across the repo (120 problems) — unrelated to this fix; worth a separate `lint cleanup` issue if not already tracked.
- Pre-existing CLAUDE.md known issues still open: NumberParser bug (`'42'` → `'4'`), `setToolMessage`/`setToolCallMessage` not returning `this`.

# docs agent — 2026:08:14T11:01:56

- **Branch**: agent/docs/2026-08-14
- **Started**: 2026-08-14T11:01:56Z
- **Finished**: 2026-08-14T11:07:00Z
- **Status**: complete

## Summary

- No open issues labeled `documentation`, so I picked up the largest undocumented
  behavior change in recent `src/` history: `fix(request): do not retry
  deterministic configuration/prompt/auth errors (#723)`.
- The docs still implied `numOfAttempts` applies to every failure. Added a
  **Retry Behavior** section to `docs/misc/errors.md` covering which categories
  short-circuit `backOff`, and the easy-to-miss distinction that
  `llm.provider_auth_failed` is category `llm` (retried) while
  `auth.aws_signing_input_missing` is category `auth` (not retried) — the category
  is the code prefix.
- Added a "What actually gets retried" subsection to the retries-and-timeouts
  concept page and a pointer on the `numOfAttempts` row in `docs/llm/generic.md`,
  both linking to the new anchor.
- Regenerated `docs/public/llms.txt` and `llms-full.txt` via
  `scripts/generate-llms-txt.sh`; `npm run typecheck` passes.
- Verified against `src/utils/modules/requestWrapper.ts` and
  `src/errors/knownCodes.ts` rather than the commit message alone.

## Files Changed

- `docs/misc/errors.md` (new Retry Behavior section)
- `docs/examples/concepts/retries-and-timeouts.md` (what actually gets retried)
- `docs/llm/generic.md` (`numOfAttempts` row note)
- `docs/public/llms.txt`, `docs/public/llms-full.txt` (regenerated)

## Next Steps

- **`tool_choice` + extended thinking (#720).** `src/llm/openai/...`/anthropic now
  guards forced `tool_choice` against extended thinking, and named `tool_choice`
  was fixed. Neither `docs/llm/anthropic.md` nor
  `docs/executor/openai-functions.md` mentions `tool_choice` at all — a grep for
  `tool_choice|toolChoice` across docs returns nothing. Worth a short section on
  how tool choice is expressed and what happens when `effort` enables thinking.
- **Bedrock model identifiers (#719, #722).** `canonicalAnthropicModel` now gates
  inference-profile / foundation-model ARNs, and the invoke endpoint URL-encodes
  the model id. Check whether `docs/llm/bedrock/anthropic.md` documents passing an
  ARN or inference profile as `model`.
- **Error docs depth.** `docs/misc/errors.md` lists categories but not the full
  code table; a code→category→retryable table would make the page self-contained.
- Confirm nothing else in the docs promises retry-on-everything — I only checked
  the three files above plus `docs/executor/hooks.md` references.

# docs-sync agent - 2026:06:22T17:00:50

- **Branch**: agent/docs-sync/2026-06-22
- **Started**: 2026-06-22T17:00:50Z
- **Finished**: in progress
- **Status**: running

## Summary

- `a11y-docs.yml` bumped its `actions/checkout` pin from `v4` to `v5`. Propagated the new pin into the A11y deep dive (single-job DAG, sequence diagram, external-calls flowchart) and into section 9.19 of `WORKFLOW_ARCHITECTURE.md`.
- `personas-run.yml` raised `--max-turns` from 40 to 80 for both persona and curator jobs. Updated `PERSONAS_RUN_DEEP_DIVE.md` (external-calls box, code block, state-machine label, quick-reference card) and rewrote the lower-than-50 comparison note so it now explains the higher budget is intentional.
- Same workflow added an `actions/upload-artifact@v7` step that uploads `/tmp/agent-prompt.txt` (7-day retention) on both the persona and curator jobs. Documented it in the run-curator step DAG, the sequence diagram (both legs), and added a Quick Reference row.
- Bumped the persona/curator turn counts (`40` to `80`) in section 7.3 of `WORKFLOW_ARCHITECTURE.md` and added an Artifact note to the section 9.3 catalog entry.
- Corrected stale source-line ranges in section 5 of `PERSONAS_RUN_DEEP_DIVE.md` (workflow file grew with the new upload steps).

## Files Changed

Docs updated:
- `.github/docs/A11Y_DOCS_DEEP_DIVE.md` (synced to `.github/workflows/a11y-docs.yml`)
- `.github/docs/PERSONAS_RUN_DEEP_DIVE.md` (synced to `.github/workflows/personas-run.yml`)
- `.github/docs/WORKFLOW_ARCHITECTURE.md` (a11y catalog entry + personas catalog entry + tool/turns table)

## Next Steps

- The persona/curator `id-token: write` permission is declared in the workflow but not mentioned in `PERSONAS_RUN_DEEP_DIVE.md` section 12 (Quick reference). Quick-ref says `contents/PR/issues: write, id-token: write`, which is accurate, but the deep dive prose does not explain why `id-token: write` is requested. Worth a one-line callout next pass.
- The persona job's `git config user.email` now uses `vars.APP_BOT_USER_ID`, not `vars.APP_CLIENT_ID`. The deep dive does not surface this detail anywhere; if a security/identity audit doc lives under `.github/docs/`, it may need a parallel update.
- `WORKFLOWS_INDEX.md` was not touched; the personas/a11y triggers, permissions, and timeouts are unchanged so no index row needed updating. Worth a quick verification next run.
- The `claude_args` block now passes `--allowedTools` as a quoted string and trailing whitespace on each line. The deep dive shows the same shape; no action needed, just flagging in case anyone reformats.

# docs agent — 2026:09:25T15:06:39

- **Branch**: agent/docs/2026-09-25
- **Started**: 2026-09-25T15:06:39Z
- **Finished**: 2026-09-25T15:13:00Z
- **Status**: complete
- **PR**: #818 (draft, → development)

## Summary

- Worked the two open `documentation` issues rather than free-form doc review.
- **#804 (fixed)** — `docs/executor/hooks.md` listed `executionMetadata.handlerOutput` as available on `onSuccess`/`onComplete` only, implying token usage was unrecoverable in `onError` after a parser failure. Verified against `src/executor/_base.ts`: `handlerOutput` is set immediately after the handler resolves, *before* `getHandlerOutput` runs the parser, so it survives into the `catch` and `finally` paths. Added `onError` to the table plus a **Reading usage after a parser failure** section: usage-recovery example, an availability table keyed to execution progress, and guidance to account once in `onComplete` instead of double-counting across hooks.
- **#755 (no change — recommended for closure)** — the issue asks to document `xai.grok-4.20-reasoning` as supporting `effort`, contingent on PR #754 merging. PR #754 was **closed, not merged**, and `isReasoningModel` in `src/llm/config/x/index.ts` still matches only `grok-4.3`/`grok-4.5`/`grok-4.6`. `docs/llm/xai.md` already documents exactly that, including a warning callout. Making the edit would have documented behavior the library does not have. Commented on the issue with evidence.
- Verified empirically with a throwaway test (created, run, deleted — not committed): on a JSON parser failure against the mock provider, `handlerOutput` is defined in both `onError` and `onComplete`, and `getResult().usage` reads cleanly alongside a populated `error`.
- Caught a typing error in my own first draft: `getResult` is `() => OutputResult` with no type parameter, so the examples use `getResult().usage`, not `getResult<OutputResult>()`. `npm run typecheck` passes.

## Files Changed

- `docs/executor/hooks.md` — modified (only committed source change)
- `scripts/agents/logs/docs/2026-09-25T15-06-39.md` — this log

## Next Steps

- **#755 needs a maintainer decision.** It should be closed, or explicitly parked behind a replacement for the closed PR #754. The doc edit only becomes valid once `isReasoningModel` actually covers `grok-4.20-0309-reasoning`. Do not action it as written.
- `docs/llm/xai.md` mentions `grok-4.6` in the shorthand table and `effort` row, but the deprecation table and prose still point migrations at `grok-4.3`. Worth a pass to confirm `grok-4.6` is the intended default recommendation now.
- Broader audit not started this run — `docs/` has ~60 markdown files and only `docs/executor/hooks.md` was reviewed in depth. Highest-value next targets: `docs/executor/options.md` and `docs/executor/openai-functions.md` (hook/metadata claims likely overlap with the #804 correction and may repeat the same error).
- Consider whether the `handlerOutput`-on-error guidance belongs in `docs/misc/errors.md` too, so cost-accounting readers find it from the error-handling entry point.
- Not verified this run: whether `examples/` still compiles against 3.0.5. `examples/` has its own `package.json` and was not exercised.

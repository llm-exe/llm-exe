# tester agent — 2026:09:14T15:44:34

- **Branch**: agent/tester/2026-09-14
- **Started**: 2026-09-14T15:44:34Z
- **Finished**: 2026-09-14T15:47:00Z
- **Status**: complete

## Summary

- Re-surveyed coverage. Only **3 files** remain below 100% on any metric, and
  all three gaps are the unreachable defensive branches already documented by
  the 2026-08-27 run. The previous run's "still genuinely open" list is now
  mostly stale: `_utils.parseHeaders.ts` and `JsonParser.ts` are at 100%, and
  `src/llm/request.ts` no longer exists (the file is `src/utils/modules/request.ts`).
- Confirmed the last live-looking item is **not** reachable either:
  `src/utils/modules/request.ts:117` (`safeProviderString(detail) ?? ""`). The
  `?? ""` arm needs `detail` to be a non-string or empty string, but the guard
  at line 103 makes `bodyText` truthy and every rung of the `b.error?.message
  || b.error || b.message || bodyText` chain falls back to that truthy string.
  Did not write a test that can't pass.
- Added a **mutation-safety contract** for `anthropicPromptSanitize` — the one
  real assertion gap I found in an otherwise well-covered module. The sanitizer
  runs once per HTTP attempt and llm-exe retries with the same prompt object,
  so input mutation would corrupt only the *retry* body: a bug that hides until
  the exact moment it does the most damage. Three tests pin it: no mutation when
  `mergeConsecutiveSameRole` combines array content, no mutation when a leading
  system message is hoisted to `_outputObj.system` (and mid-conversation system
  roles are rewritten to `user`), and calling the sanitizer twice on the same
  messages yields identical output and identical output objects.
- No bug found — the shallow copy + array reassignment is correct today. These
  tests exist to keep it correct.
- Full suite green: **199 suites / 3144 tests**, `npm run typecheck` clean.
- No GitHub issues filed. No open issues labeled `testing`.

## Files Changed

- `src/llm/config/anthropic/promptSanitize.test.ts` — added a
  `caller input is not mutated` describe block (3 tests).
- `scripts/agents/logs/tester/2026-09-14T15-44-34.md` — this log.

## Next Steps

**Line/branch coverage is exhausted — stop chasing it.** The three remaining
sub-100% entries are all confirmed dead or unreachable:

- `src/utils/modules/redactSecrets.ts:26` — `maskToken`'s `match.length <= 8`
  guard is dead (shortest regex alternative is 12 chars) and `maskToken` isn't
  exported. Unreachable unless the regex changes.
- `src/utils/modules/request.ts:117` — see above, unreachable.
- `src/errors/index.ts` — 83% *function* coverage: some re-exported functions
  are never called through the barrel. Cosmetic; not worth a test.

Future runs should keep auditing **assertion quality** in already-covered code.
Concrete leads I did not get to:

- **Other prompt transforms deserve the same mutation-safety treatment.**
  I only pinned Anthropic's. The OpenAI, Google, xAI, and Bedrock transforms in
  `src/llm/config/*` also shallow-copy caller messages and run once per retry —
  same failure mode, same test shape. This is the highest-value follow-up, and
  the tests I added are a direct template.
- Tests asserting only on error-message regexes rather than
  `code`/`category`/`context` — `grep -rn '\.toThrow(/' src/` to find them.
  Message text is not the public contract; the error code is.
- Provider configs where `mapBody` is invoked but the transformed output shape
  isn't deeply asserted.
- A retry-path integration test: drive a provider through a simulated failure +
  retry and assert the second request body byte-identical to the first. That
  would cover at the executor level what I covered at the unit level.

Still open from CLAUDE.md's Known Issues: the worker-process force-exit warning.
The 2026-08-27 run traced the timer clearing in `asyncCallWithTimeout.ts` and
found it correct, suspecting pending promises in the test file itself. Not
chased this run; note that the warning did **not** appear in this run's output.

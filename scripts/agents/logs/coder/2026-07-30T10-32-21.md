# coder agent — 2026:07:30T10:32:21

- **Branch**: agent/coder/2026-07-30-issue-231
- **Started**: 2026-07-30T10:32:21Z
- **Finished**: 2026-07-30T10:37Z
- **Status**: complete

## Summary

- Worked issue #231 — three Google shorthands (`google.gemini-2.0-flash`, `google.gemini-2.0-flash-lite`, `google.gemini-1.5-pro`) pointed at models Google has fully shut down (2.0 line on 2026-06-01, 1.5-pro on 2025-09-29) and were registered as plain live entries, so callers got a raw API error with no prior warning.
- Wrapped all three in `deprecateShorthand()` with the shutdown date and a named migration target, matching the treatment the 2.5 shorthands already receive.
- Deliberately did *not* remove the keys: that changes `AllUseLlmOptions` and is a breaking type change belonging on v4.0.0. Left an inline comment marking them for removal.
- Added a table-driven `retired shorthands` test asserting each still resolves to its model, carries `deprecated.shorthand`, and names the migration target in its message.
- Verified: 194 suites / 2672 tests pass, `npm run typecheck` clean. `npm run lint` reports 130 errors, all pre-existing and none in the touched files (the two in `src/llm/config/google/` are `no-undef` on `fail` in `promptSanitize.test.ts`, untouched).

## Files Changed

- `src/llm/config/google/index.ts` — wrapped the three retired shorthands in `deprecateShorthand()`
- `src/llm/config/google/google.test.ts` — added `retired shorthands` coverage

## Next Steps

- **Removal pass for v4.0.0.** The three keys are now warning-wrapped but still shipped. Dropping them (and the orphaned entries in `AllUseLlmOptions`) is the real fix and needs the `breaking` label + v4.0.0 milestone. Same applies to the Anthropic retirements tracked in #679.
- **The 2.5 shorthands are now past their shutdown dates too.** `google.gemini-2.5-pro` and `google.gemini-2.5-flash` were slated for 2026-06-17 and `gemini-2.5-flash-lite` for 2026-07-22 — all in the past as of this run. Their deprecation messages still read "will shut down on ...", future tense, which is now wrong. Worth a follow-up to restate them as shut down and point at the 3.x line; there may already be an issue tracking the 2.5 deprecations (the scout mentioned filing one on 2026-05-04).
- **Repo-wide lint debt.** 130 pre-existing eslint errors on `main`, mostly `no-undef` on Jest's `fail`, `no-empty`, and `no-prototype-builtins`. `npm run lint` is effectively non-gating right now. Out of scope here but someone should either fix or re-baseline the config.

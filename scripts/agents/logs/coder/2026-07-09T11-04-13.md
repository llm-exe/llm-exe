# coder agent — 2026:07:09T11:04:13

- **Branch**: agent/coder/2026-07-09-issue-652
- **Started**: 2026-07-09T11:04:13Z
- **Finished**: 2026-07-09T11:12:00Z
- **Status**: complete

## Summary
- Fixed issue #652: `redactSecrets` leaked `x-api-key` when it appeared as a JSON field (`{"x-api-key":"..."}`).
- Root cause: the JSON-shaped regex used `api[-_]?key`, which cannot match `x-api-key` because the `x-` prefix sits between the opening quote and `api`.
- Added `x-api-key` explicitly to the JSON pattern alternation, mirroring the header/query pattern which already listed it.
- Flipped the existing `it.failing` repro in `redactSecrets.test.ts` to a passing test asserting full redaction (`{"x-api-key": "[redacted]"}`).
- Verified: redactSecrets suite (43 tests) passes, `npm run typecheck` clean. Lint errors present are pre-existing repo-wide (none in changed files).

## Files Changed
- `src/utils/modules/redactSecrets.ts` — added `x-api-key` to the JSON redaction pattern
- `src/utils/modules/redactSecrets.test.ts` — flipped failing repro to passing test with exact-output assertion

## Next Steps
- Repo-wide lint has 124 pre-existing errors (e.g. `no-prototype-builtins` `hasOwnProperty` access) unrelated to this issue — worth a dedicated cleanup pass.
- Consider auditing whether other multi-word header names (e.g. `x-goog-api-key`) share the same JSON-pattern blind spot.

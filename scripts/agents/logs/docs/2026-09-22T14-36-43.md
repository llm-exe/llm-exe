# docs agent — 2026:09:22T14:36:43

- **Branch**: agent/docs/2026-09-22
- **Started**: 2026-09-22T14:36:43Z
- **Finished**: 2026-09-22T14:45:00Z
- **Status**: complete

## Summary

- Addressed issue #804: `docs/executor/hooks.md` claimed `executionMetadata.handlerOutput` was only available on `onSuccess`/`onComplete`. Verified against `src/executor/_base.ts` — `handlerOutput` is recorded before `getHandlerOutput()` (where parsing happens), so it is present in `onError` and `onComplete` after a parser failure.
- Corrected the hook metadata table rows for `handlerInput` and `handlerOutput` to describe availability by execution progress rather than by hook name.
- Added a "What's Available When" section with a table mapping how far execution got (input transform → provider call → parse → success) to which metadata fields are populated, plus guidance that `handlerOutput` must be guarded because it is optional.
- Added a typed `onComplete` example that recovers token usage via `exec.handlerOutput?.getResult().usage` after a parse failure, and a note to use `onComplete` as the single accounting point so successes and parse failures are not double-counted. Verified the example compiles (scratch file + `npm run typecheck`, removed after): `handlerOutput` and `OutputUsage` infer with no casting when hooks are passed via the `hooks` option.
- Re-checked issue #755 (xAI `effort` on `xai.grok-4.20-reasoning`): still blocked. PR #754 is closed-unmerged and PR #776 is still open, so `isReasoningModel` in `src/llm/config/x/index.ts` covers only `grok-4.3`, `grok-4.5`, `grok-4.6`. `docs/llm/xai.md` (including the #732 warning callout and the `effort` row) is accurate as-is — no change made.

## Files Changed

- `docs/executor/hooks.md` — corrected availability of `handlerInput`/`handlerOutput` in the hook metadata table; added "What's Available When" section, the usage-after-parse-failure example, and the double-counting guidance.
- `scripts/agents/logs/docs/2026-09-22T14-36-43.md` — this log.

## Next Steps

- **Typing gap in `.on()`**: hooks registered with `.on()` / `.once()` take `ListenerFunction = (...args: any[]) => void` (`src/interfaces/functions.ts:7`), so the `executionMetadata` parameter is `any` — no inference on `handlerOutput`, `output`, or `input`. Hooks passed through the `hooks` option are fully typed via `CoreExecutorHookInput`. This is a real type-safety leak for the most common registration path; worth a source-side issue for the coder agent (docs now work around it by recommending the `hooks` option).
- **#755 remains blocked** on #732 / PR #776. When a fix merges, the edit in `docs/llm/xai.md` is: delete the `effort` warning callout and add `grok-4.20-0309-reasoning` / `xai.grok-4.20-reasoning` to the `effort` row of the xAI-Specific Options table.
- `docs/llm/index.md` "Token usage and prompt caching" uses `.on("onSuccess", ...)` for its usage example. Now that `hooks.md` documents `onComplete` as the single accounting point, that example could be aligned (or cross-linked) on a future run.
- Not reviewed this run: `docs/executor/execution-context.md`, `docs/parser/*`, `docs/state/*`, and `examples/`. The hook-adjacent files (`execution-context.md`, `options.md`) are the natural next target since they share the metadata vocabulary just corrected.

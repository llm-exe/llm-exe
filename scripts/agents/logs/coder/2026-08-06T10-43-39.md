# coder agent — 2026:08:06T10:43:39

- **Branch**: agent/coder/2026-08-06-issue-705
- **Started**: 2026-08-06T10:43:39Z
- **Finished**: 2026-08-06T10:49:00Z
- **Status**: complete — [PR #728](https://github.com/llm-exe/llm-exe/pull/728) (draft)

## Summary

- Worked assigned issue #705 — three OpenAI shorthands sit on snapshots in the December 11, 2026 shutdown batch.
- Wrapped `openai.gpt-5-mini`, `openai.gpt-5-nano`, and `openai.o3` in `deprecateShorthand()`, following the Gemini 2.5 pattern in `src/llm/config/google/index.ts`. Messages name the `2026-12-11` date and the OpenAI-recommended replacement (`openai.gpt-5.6-terra`, `openai.gpt-5.6-luna`, `openai.gpt-5.6`).
- **Non-breaking** — the shorthands resolve to the same models and behave identically; the change only stamps a `deprecated` payload so users get a one-time `DeprecationWarning`. Nothing removed.
- Did **not** resolve the issue's caveat about undated aliases vs. dated snapshots — could not verify against the live API from this environment, so I did not guess. A warning is the safe posture either way; flagged in the PR that a human should confirm before going further than a warning.
- Verified: `npm test` (196 suites / 2925 tests, all pass), `npm run typecheck` clean, `npm run lint` no new errors in touched files.

## Files Changed

- `src/llm/config/openai/index.ts` — moved the three shorthands into the `// Deprecated` block with `deprecateShorthand()` + an explanatory NOTE comment.
- `src/llm/config/openai/openai.test.ts` — added the three to the `deprecated shorthands still resolve` block; new tests asserting each carries its own `deprecated` payload with the correct shorthand/date/replacement, plus a guard that the 5.6 replacements are not themselves deprecated.

## Next Steps

- **Verify the alias question (#705).** Whether `gpt-5-mini` / `gpt-5-nano` / `o3` re-point to newer snapshots or die with them determines if these shorthands need removal (breaking, next major) or if the deprecation should be reverted. Needs a human with API access — agents should not guess.
- **`reasoning.mode: pro` shape change.** Noted at the end of #705 and not yet filed as its own issue. OpenAI's replacement column suggests the pro tier is now a reasoning parameter rather than a separate `-pro` model ID. The `isReasoningModel` prefix match on `gpt-5`/`o3`/`o4` in `src/llm/config/openai/index.ts` still holds, but the parameter side may need work. Worth reading the reasoning docs and filing properly.
- **#704 is still open** — `openai.gpt-4.1-nano` (and likely `openai.gpt-4`) shut down October 23, 2026, which is the nearer deadline. Same playbook, straightforward follow-up run. It carries the same undated-alias caveat for `openai.gpt-4`.
- **Pre-existing lint debt** — `npm run lint` reports 130 errors repo-wide (`no-empty`, `no-prototype-builtins`, etc.), none in the files touched here. Unrelated to this work but worth a dedicated cleanup pass.

# docs-sync agent - 2026:06:04T15:17:19

- **Branch**: agent/docs-sync/2026-06-04
- **Started**: 2026-06-04T15:17:19Z
- **Finished**: 2026-06-04T15:25:00Z
- **Status**: complete

## Summary

- Synced [BOT_RESPOND_DEEP_DIVE.md](../../../../.github/docs/BOT_RESPOND_DEEP_DIVE.md) to reflect the `--max-turns` bump from 30 to 50 in [bot-respond.yml](../../../../.github/workflows/bot-respond.yml) (commit `45b67a7`).
- Updated 7 references inside the deep dive: the section-1 overview, the one-job DAG, the external-calls block, the tool-allowlist code fence, the state-machine timeout edge, the failure-modes node, and the quick reference card.
- Reworked the comparative sentence in section 8 that previously claimed bot-respond ran on a "lower turn budget (30 vs 50)" than `agent-run.yml`; both are now 50, so the new prose notes the parity and the historical context.
- Synced [WORKFLOW_ARCHITECTURE.md](../../../../.github/docs/WORKFLOW_ARCHITECTURE.md) section 5.5 (tool allowlist table row for `bot-respond`) and section 9.6 (bot-respond catalog entry) to the new 50-turn budget.
- Verified the modified files contain no em or en dashes via hex regex per the run prompt.

## Files Changed

- `.github/docs/BOT_RESPOND_DEEP_DIVE.md` (synced against `.github/workflows/bot-respond.yml`)
- `.github/docs/WORKFLOW_ARCHITECTURE.md` (synced against `.github/workflows/bot-respond.yml`)

## Next Steps

- None blocking. The single source-file change was fully reflected in the two deep-dive docs that name a turn budget for `bot-respond`. No other deep dive references this value.
- Future drift to watch: the `Earlier this workflow ran with a tighter budget of 30` line in section 8 of `BOT_RESPOND_DEEP_DIVE.md` is a historical note. If the budget is bumped again, consider trimming that sentence so the doc does not collect a paper trail of past values.

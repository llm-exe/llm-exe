# docs agent — 2026:09:04T14:08:59

- **Branch**: agent/docs/2026-09-04
- **Started**: 2026-09-04T14:08:59Z
- **Finished**: 2026-09-04T14:18:00Z
- **Status**: complete

## Summary

- Checked assigned work: issue #755 (xai `effort` support for `xai.grok-4.20-reasoning`) is still **blocked** — PR #754 was closed without merging, and `isReasoningModel` in `src/llm/config/x/index.ts` still covers only `grok-4.3`/`grok-4.5`/`grok-4.6`. Left the existing warning callout as-is; no doc change made.
- `docs/llm/xai.md`: split the shorthand table into active vs. deprecated. Six shorthands (`xai.grok-2`, `xai.grok-3`, `xai.grok-3-mini`, `xai.grok-4`, `xai.grok-4-fast`, `xai.grok-4-1-fast`) are deprecated in source but were listed as normal options; two were used in the usage examples. Examples now use `xai.grok-4.3` / `xai.grok-4.20`.
- `docs/llm/gemini.md`: examples moved off the deprecated `google.gemini-2.5-flash` to `google.gemini-3.5-flash`; corrected the "Gemini-Specific Options" NOTE, which claimed `temperature`/`maxTokens`/`topP` are not mapped — the config maps all of them plus `stopSequences`.
- Documented that `effort` is silently dropped on every current Gemini model (the transform allowlists only `gemini-2.5-*`, all deprecated shorthands) and filed it as a source bug: **#781** (`bug` label, not fixed here — docs-only scope).
- Swapped deprecated shorthands out of `docs/index.md` and the `examples/` files that used them. `npm run typecheck` passes.

## Files Changed

- `docs/llm/xai.md`
- `docs/llm/gemini.md`
- `docs/index.md`
- `examples/basicTools.ts`
- `examples/writeTestsFromCode.ts`
- `examples/claude.md`

## Next Steps

- **Deprecation drift is systemic, not xAI-specific.** `src/llm/config/*` now deprecates 23 shorthands across openai/anthropic/google/xai. Only `docs/llm/xai.md` marks them. `docs/llm/openai.md` and `docs/llm/anthropic.md` should get the same active-vs-deprecated treatment, and `docs/llm/gemini.md` has no shorthand table at all.
- `docs/public/llms-full.txt` is stale — it still carries the old xAI table and examples. Find out whether it is generated (and by which script) before hand-editing; if it is generated, regenerate it after these doc changes land.
- `docs/.vitepress/utils/providers.json` lists deprecated shorthands with no deprecation flag, so the `<ImportModelNames />` component renders them as normal choices. Worth checking how that file is produced and whether it can carry a deprecated marker.
- Issue #755 stays blocked on #732. When a fix merges, remove the `effort` warning callout in `docs/llm/xai.md` and add `grok-4.20-0309-reasoning` to the supported list.
- Same for #781: once the Gemini `effort` allowlist is updated, drop the warning callout in `docs/llm/gemini.md`.
- Not reviewed this run: `docs/parser/`, `docs/executor/`, `docs/prompt/`, `docs/state/`, `docs/callable/`, `docs/embeddings/`.

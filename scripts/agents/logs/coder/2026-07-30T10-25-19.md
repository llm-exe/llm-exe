# coder agent — 2026:07:30T10:25:19

- **Branch**: agent/coder/2026-07-30-issue-696
- **Started**: 2026-07-30T10:25:19Z
- **Finished**: 2026-07-30T10:29:00Z
- **Status**: complete

## Summary

- Worked issue #696 (enhancement, milestone v3.1.0): added the two missing Google shorthands.
- Added `google.gemini-3.6-flash` and `google.gemini-3.5-flash-lite` to `src/llm/config/google/index.ts` via `withDefaultModel(googleGeminiChatV1, ...)`, following the existing 3.x shorthand pattern exactly — no config divergence from the base chat config.
- Added matching `AllUseLlmOptions` keys in `src/interfaces/llm.ts` so typed shorthand inference resolves both.
- Added two `describe` blocks in `google.test.ts` mirroring the existing `gemini-3.5-flash` / `gemini-3.1-flash-lite` tests (base-config inheritance + model default override).
- Verified: `npm test` 194 suites / 2673 tests pass, `npm run typecheck` clean. `npm run lint` reports 131 problems, but that is the pre-existing baseline (identical count with changes stashed) and none are in the files touched here.
- Skipped the preview models (Gemini 3.1 Pro, Gemini 3 Flash) per the project's no-shorthands-for-preview rule.

## Files Changed

- `src/llm/config/google/index.ts` — two new `withDefaultModel` shorthand entries
- `src/interfaces/llm.ts` — two new `AllUseLlmOptions` keys
- `src/llm/config/google/google.test.ts` — two new `describe` blocks

## Next Steps

- **Unverified model IDs (flagged on the PR).** No `GEMINI_API_KEY` in this environment, so `GET /v1beta/models` could not be called to confirm the raw ID strings, as the issue asked. `gemini-3.6-flash` and `gemini-3.5-flash-lite` follow Google's documented naming convention and every existing shorthand here, but a wrong ID fails at call time, not build time — a maintainer should eyeball this before merge. The PR is a draft for that reason.
- **`effort` mapping is stale for 3.x models.** In `src/llm/config/google/index.ts`, the `mapBody.effort` transform gates its thinking-budget mapping on a hardcoded allowlist of `["gemini-2.5-pro", "gemini-2.5-flash", "gemini-2.5-flash-lite"]` — all three of which are now deprecated shorthands. That means `effort` is silently dropped for every 3.x model, including the two added here. Worth its own issue and its own PR; deliberately left out of scope for this run.
- Preview models 3.1 Pro / 3 Flash can get shorthands once Google marks them stable.

# coder agent — 2026:09:03T12:38:49

- **Branch**: agent/coder/2026-09-03-issue-775
- **Started**: 2026-09-03T12:38:49Z
- **Finished**: 2026-09-03T12:42:00Z
- **Status**: complete — PR #778 (draft, base `development`)

## Summary

- Fixed #775: the `effort` entry in `google.chat.v1` mapped to `config.thinkingConfig.thinkingBudget`. `mapBody` treats `key` as a literal dot path into the outgoing REST body (`convertDotNotation`), so the budget was emitted as a **top-level `config` object** rather than under `generationConfig`, where the `v1beta/.../generateContent` endpoint expects it. `config` is the Google GenAI SDK wrapper's field name, not the REST one.
- One-line change to `generationConfig.thinkingConfig.thinkingBudget`, matching how every other generation setting in the same config (`temperature`, `topP`, `maxTokens`, `stopSequences`) already targets `generationConfig.*`. The `transform` function was left untouched.
- Root cause of the gap in coverage: existing tests only exercised the `effort` transform in isolation, never the assembled body. Added a `google.chat.v1 effort body mapping` block with four `mapBody`-level tests (key path; `thinkingBudget` nested under `generationConfig` with no top-level `config`; coexistence with other `generationConfig` params; unsupported model emits neither). The first three fail against the old key.
- Verified: `npm test` 199 suites / 3145 tests pass, `npm run typecheck` clean. `npm run lint` reports 130 errors repo-wide, **all pre-existing** and none in the touched files (nearest is `promptSanitize.test.ts`, untouched).
- Posted the plan on #775 before writing code, per instructions.

## Files Changed

- `src/llm/config/google/index.ts` — `effort` mapBody key `config.*` → `generationConfig.*`
- `src/llm/config/google/google.test.ts` — new `google.chat.v1 effort body mapping` describe block (4 tests)
- `scripts/agents/logs/coder/2026-09-03T12-38-49.md` — this log

## Next Steps

- **Not live-verified.** I could not confirm against a real Google API call whether the old top-level `config` field was silently ignored or rejected with a 400 — #775 flagged this as worth checking and it determines whether this was a silent no-op or an outright failure. Either way `thinkingBudget` was not reaching the model. Worth a single live call before the PR leaves draft.
- **#772 (already open, do not re-file).** The `effort` transform's model allowlist is `gemini-2.5-pro` / `gemini-2.5-flash` / `gemini-2.5-flash-lite` — all three deprecated — so `effort` still returns `undefined` on every current `google.gemini-3.*` shorthand. My fix corrects *where* the budget lands; #772 covers *whether it is produced at all* on 3.x. **These two need to land together to be user-visible** — #775 alone changes nothing for anyone on a 3.x model. Recommend the next run picks up #772.
- #762 (gemini-2.5-* deprecation warnings cite shutdown dates that never happened) is in the same config file and would pair cleanly with #772 as a single Google-config cleanup pass.
- Related unclaimed provider-option bugs following the same "option silently dropped / wrong key" shape: #750 (openai `maxTokens` → `max_tokens` unconditionally, 400s on reasoning models), #732 (xai grok-4.20-reasoning drops `effort`), #662 (DeepSeek rejects `json_schema` response_format). #750 looks like the highest-impact of these.

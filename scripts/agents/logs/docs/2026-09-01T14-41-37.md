# docs agent — 2026:09:01T14:41:37

- **Branch**: agent/docs/2026-09-01
- **Started**: 2026-09-01T14:41:37Z
- **Finished**: 2026-09-01T14:49:00Z
- **Status**: complete
- **PR**: https://github.com/llm-exe/llm-exe/pull/777

## Summary

- Checked assigned work: issue #755 (`documentation`) is **still blocked** and was not actioned. PR #754 was closed without merging, so `isReasoningModel` in `src/llm/config/x/index.ts:11-12` still matches only `grok-4.3`/`grok-4.5`/`grok-4.6`. Listing `xai.grok-4.20-reasoning` as supporting `effort` would document behavior that does not exist. Three prior runs reached the same conclusion, so I did not add a fourth duplicate comment to the issue — the PR body records the finding instead.
- Fixed real drift in `docs/llm/xai.md`: six shorthands (`xai.grok-2`, `xai.grok-3`, `xai.grok-3-mini`, `xai.grok-4`, `xai.grok-4-fast`, `xai.grok-4-1-fast`) are wrapped in `deprecateShorthand` in source but were documented as normal options.
- Split the xAI model table into active and deprecated sections, with the reason for each deprecation (retired-and-redirected by xAI on 2026-05-15 vs. delisted), and linked to `/llm/deprecations`.
- Switched the two xAI usage examples off `xai.grok-4` and `xai.grok-3-mini` — copying the docs verbatim emitted a deprecation warning.
- Verified the existing `effort` warning callout for `xai.grok-4.20-reasoning` is still accurate against source and left it in place.

## Files Changed

- `docs/llm/xai.md` (modified)
- `scripts/agents/logs/docs/2026-09-01T14-41-37.md` (this log)

## Next Steps

1. **Same deprecation drift in three more provider docs.** `docs/llm/openai.md`, `docs/llm/anthropic.md`, and `docs/llm/gemini.md` contain no mention of deprecation at all, yet source deprecates:
   - openai: `openai.gpt-4.1-nano`, `openai.gpt-4`, `openai.gpt-5-mini`, `openai.gpt-5-nano`, `openai.o3`, `openai.o4-mini`
   - anthropic: `anthropic.claude-opus-4-6`, `anthropic.claude-3-7-sonnet`, `anthropic.claude-3-5-sonnet`, `anthropic.claude-3-5-haiku`, `anthropic.claude-3-opus`
   - google: `google.gemini-2.5-flash`, `google.gemini-2.5-flash-lite`, `google.gemini-2.5-pro`, `google.gemini-2.0-flash`, `google.gemini-2.0-flash-lite`, `google.gemini-1.5-pro`

   Apply the same active/deprecated table split used in `xai.md`, and check each doc's usage examples for deprecated shorthands. This is the highest-value next task — one provider per run is a comfortable size.
2. **Issue #755 stays blocked on #732.** Do not touch the `effort` list in `xai.md` until a fix for #732 actually merges; re-check `isReasoningModel` in `src/llm/config/x/index.ts` before making any edit. When it does merge, remove the warning callout and add `grok-4.20-0309-reasoning` to the `effort` row. Consider whether #755 should just be closed as duplicate-of-blocked to stop it re-consuming a run's budget.
3. **Not reviewed this run** (time budget): `docs/parser/`, `docs/executor/`, `docs/state/`, `docs/embeddings/`, and the `examples/` directory. The `src/index.ts` export surface was read but not audited doc-by-doc against the docs tree — worth a dedicated run to confirm every export is documented (notably the error helpers `isLlmExeError`, `serializeLlmExeError`, `formatLlmExeErrorForLog` and the newer parser option types).

# docs agent — 2026:08:11T10:56:19

- **Branch**: agent/docs/2026-08-11
- **Started**: 2026-08-11T10:56:19Z
- **Finished**: 2026-08-11T11:03:00Z
- **Status**: complete

## Summary

- No open `documentation`-labeled issues, so worked from a diff of recent source changes against the docs.
- Found a real gap from #723 (`fix(request): do not retry deterministic configuration/prompt/auth errors`): the retry short-circuit was never documented, so `numOfAttempts` read as if it applied to every failure. Documented it in the three places a user would look.
- Added the missing `unknown` entry to the error-category list in `docs/misc/errors.md` (`src/errors/types.ts` has 13 categories; the doc listed 12).
- Audited the public export surface in `src/index.ts` against the docs — every export is covered somewhere except `LlmNativeFunctionParser`, which is deprecated and whose documented replacement is not actually exported. Filed [#741](https://github.com/llm-exe/llm-exe/issues/741) (`bug`) rather than documenting a broken migration path; no source touched.
- `npm run typecheck` passes (exit 0). Docs changes are prose-only — no code samples added or altered.

## Files Changed

- `docs/misc/errors.md` — new "Retry Behavior" section; added `unknown` to the categories list
- `docs/llm/generic.md` — `numOfAttempts` row now links to Retry Behavior
- `docs/examples/concepts/retries-and-timeouts.md` — new "Not everything gets retried" section
- `scripts/agents/logs/docs/2026-08-11T10-56-19.md` — this log

## Next Steps

- **Blocked on [#741](https://github.com/llm-exe/llm-exe/issues/741).** Once `LlmFunctionParser` is exported, document it in `docs/parser/included-parsers.md` and `docs/executor/openai-functions.md`, and note that `LlmNativeFunctionParser` / `OpenAiFunctionParser` are deprecated with different return shapes (`{ name, arguments }` vs normalized `OutputResultContent[]`) — that difference will bite anyone migrating.
- `docs/llm/deprecations.md` only covers deprecated *model shorthands*. There is no single page listing deprecated *exports*. Worth considering once #741 resolves.
- `docs/public/llms-full.txt` is generated by `scripts/generate-llms-txt.sh` via `predocs:build` — it still contains the pre-change error docs. Not hand-edited (correctly), but confirm the docs build runs before the next release so the LLM-facing bundle picks these edits up.
- Not reviewed this run, for time: `docs/embeddings/*`, `docs/state/*`, `docs/callable/*`, `docs/prompt/*`. `docs/misc/v2-to-v3-migration-guide.md` is long and was not verified against current API.
- **Biggest gap found, not addressed this run: image input is undocumented.** `image_url` content blocks are implemented for at least five providers (`src/llm/config/{openai,anthropic,google,ollama}/promptSanitize*.ts`, `src/llm/config/_utils/imageContent.ts`) and CLAUDE.md lists image input as in-scope, but `grep -rn image_url docs/` returns nothing — zero coverage. This is a shipped, supported feature no user can discover from the docs. Recommend the next run make this its main task: a page covering https URLs vs base64 `data:` URIs, which providers support it, and how the content block is built.

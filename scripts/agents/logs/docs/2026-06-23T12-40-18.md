# docs agent — 2026:06:23T12:40:18

- **Branch**: agent/docs/2026-06-23
- **Started**: 2026-06-23T12:40:18Z
- **Finished**: 2026-06-23T12:46:00Z
- **Status**: complete

## Summary

- Fixed `docs/executor/execution-context.md` "Executor Handlers" section — the example called `createCoreExecutor({name, handler})` (an unsupported config-object form) and claimed the user handler receives an `ExecutionContext` argument. In the current implementation `createCoreExecutor` takes a handler function directly, and `CoreExecutor.handler` only forwards `input` to that handler. Replaced the broken example with an accurate "Where ExecutionContext Is Available" section listing parser callbacks, hooks, and trace IDs.
- Removed the empty, unreferenced `docs/misc/getting-started.md` file.
- Verified the rest of the surveyed docs against `src/index.ts` exports and source signatures: parser list (`docs/parser/included-parsers.md`), executor options/hooks, prompts, LLM provider docs, callable wrapper, embeddings, errors, deprecations, and custom provider configuration all match the current API.
- `npm run typecheck` passes after the changes.

## Files Changed

- `docs/executor/execution-context.md` — replaced inaccurate "Executor Handlers" example with a correct overview of where `ExecutionContext` is exposed.
- `docs/misc/getting-started.md` — deleted (was a 0-byte placeholder, not referenced from `docs/.vitepress/config.mts` or any other docs page).

## Next Steps

- **Source gap to consider**: `CoreExecutor.handler` ignores the `_options`/`_context` parameters declared on `BaseExecutor.handler` and only forwards `input` to the user-supplied function. Custom parsers (`createCustomParser`) already receive `ExecutionContext` as the second arg. Surfacing the same on `createCoreExecutor` handlers would let core executors read `traceId` and executor metadata without needing a closure trick. This is a code change, not a docs change — worth filing an issue and re-documenting once decided.
- **`executor/execution-context.md` could grow**: after the source decision above, add a short worked example for each surface (parser, hook, handler) showing `traceId`/`executor.name`/`execution.input` use side-by-side.
- **Empty/short pages audit**: `docs/misc/getting-started.md` was empty. Other short pages (`docs/executor/options.md`, `docs/state/index.md`) could be expanded — `options.md` only documents `jsonSchema` at execute time; the LLM executor accepts additional options worth listing.
- **`docs/index.md` testimonials block** still contains placeholder copy ("A Developer Really Said This", "Tech Lead, Series B Fintech"). Either back with real quotes or rework the section.
- **Examples include consistency**: many docs use VitePress `<<<` includes pointing into `examples/prompt/*.ts`. A future run should sweep those for stale anchor names (`#exampleOne`, `#exampleCreateChatPromptUseFormat`, etc.) against the current example files.
- **`docs/executor/options.md` parser-default claim**: the table says parser defaults to "string parser if not provided". Worth verifying against `LlmExecutor` source on the next pass and either documenting the actual default behavior or removing the claim.

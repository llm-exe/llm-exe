# coder agent — 2026:08:20T08:38:39

- **Branch**: agent/coder/2026-08-20-issue-749
- **Started**: 2026-08-20T08:38:39Z
- **Finished**: 2026-08-20T08:43:00Z
- **Status**: complete — PR #753 opened (draft, base `development`)

## Summary

- Worked assigned issue #749: add the `xai.grok-4.6` shorthand for xAI's new flagship model. Purely additive, no behavior change to existing shorthands.
- Added `"xai.grok-4.6": withDefaultModel(xaiChatV1, "grok-4.6")` and extended `isReasoningModel` to include `grok-4.6`, following the `grok-4.5` precedent (4.6 is its direct successor). `reasoning_effort` flows through the existing shared transform in `createOpenAiCompatibleConfiguration` — no new code path, so this does not add a third variation of the #732 behavior.
- Added the matching `"xai.grok-4.6"` entry to the shorthand type map in `src/interfaces/llm.ts`. Easy to miss — without it the shorthand exists at runtime but loses typed input inference, which breaks the TypeScript-first guarantee.
- Tests: added `xai.grok-4.6` to the shorthand resolution table, a dedicated describe block, and effort transform coverage on both the valid (`minimal|low|medium|high`) and invalid (`none`, `xhigh`, non-string) paths.
- Verified: `npm test` 198 suites / 3062 tests all passing, `npm run typecheck` clean, `eslint` clean on the three changed files.

## Files Changed

- `src/llm/config/x/index.ts` — modified (shorthand entry + `isReasoningModel`)
- `src/interfaces/llm.ts` — modified (shorthand type map entry)
- `src/llm/config/x/xai.test.ts` — modified (test coverage)

## Next Steps

- **#732 — effort dropped on the grok-4.20 reasoning path.** Still open and untouched by this PR. Worth a look: `xai.grok-4.20-reasoning` defaults to model `grok-4.20-0309-reasoning`, but `isReasoningModel` only matches `grok-4.3`/`grok-4.5`/`grok-4.6` by exact string, so `reasoning_effort` is silently dropped for it. `xai.test.ts` currently *asserts* that drop as correct behavior (the "drops effort for non-reasoning xAI models" case lists `grok-4.20-0309-reasoning`), so fixing #732 means updating that test too. Not touched here — one issue per run.
- **Shorthand additions are a three-file change and nothing enforces it.** `src/llm/config/x/index.ts` and the shorthand map in `src/interfaces/llm.ts` must stay in sync, but a missing type-map entry fails silently — no test catches it. A test asserting that every key in each provider's config object has a corresponding entry in the shorthand type map would catch this class of omission for all providers, not just xAI. Would apply to the recurring "new flagship model" issues (#688, #749, and whatever comes next).
- **`isReasoningModel` for xAI is a growing exact-match string chain.** Now three `===` comparisons, and it will keep growing with each release. A prefix/set-based check would scale better, but that is a behavior change worth a maintainer's call rather than an agent's — not filed as an issue since it is speculative.
- Pre-existing repo-wide lint state: ~130 eslint errors (e.g. `no-empty` in `src/utils/modules/json.ts`, `no-prototype-builtins` in `src/utils/modules/pick.ts`). Unrelated to this work and left alone, but `npm run lint` is not currently green, so it cannot gate CI as-is.

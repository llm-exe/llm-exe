# coder agent — 2026:08:24T16:27:14

- **Branch**: agent/coder/2026-08-24-issue-757
- **Started**: 2026-08-24T16:27:14Z
- **Finished**: 2026-08-24T16:34:00Z
- **Status**: complete (partial scope — see Next Steps)

> Second run on this branch, continuing from
> [`2026-08-24T08-48-35.md`](./2026-08-24T08-48-35.md). This log was written
> retroactively during review follow-up on the same day — the run itself
> committed no log, which is what made the first log read as the branch's
> whole story. Where the two disagree, this one is current.

## Summary

- Picked up Next Steps 1–4 from the first run. Closed 2 (typed throws) and 4
  (contract fixture) outright, and closed the silently-wrong half of 1 (Google)
  without doing the real multimodal conversion. 3 (Ollama) is recorded but not
  guarded — see Next Steps.
- **Google** — `googleGeminiFunctionResponseResult`
  (`src/llm/config/google/promptSanitizeMessageCallback.ts:56`) now handles
  array content on the `function` branch instead of letting it land verbatim
  inside `functionResponse.response.result`. `response` is a Struct with no
  representation for an image part, so Gemini would have billed the base64 as
  text and never seen the image. Text-only block arrays flatten to a joined
  string (what the non-function branch effectively does for other roles); an
  image block throws `LlmExeError` / `prompt.invalid_messages` with a
  resolution. The real multimodal `functionResponse` conversion is deferred —
  the shape can't be pinned from the repo.
- **Text-only OpenAI-compatible providers** — `openaiPromptMessageCallback`
  (`src/llm/config/openai/promptSanitizeMessageCallback.ts:22`) throws the same
  typed error when a tool result carries an image block, before the
  `role: "function"` → `role: "tool"` flip. Covers `openai.chat`, `xai.chat`
  and `deepseek.chat`. Text-only block arrays still pass through untouched.
  Bedrock needed nothing: `amazon:anthropic.chat` runs the Anthropic callback
  and accepts images natively, and `amazon:meta.chat` renders to a text prompt.
- **Contract fixture** — added an `"image tool result"` row to
  `src/llm/config/promptSanitizeContract.test.ts`, forcing all nine registered
  providers to declare pass-through-or-throw. Anthropic (both hosts) keeps the
  blocks as native `tool_result.content`; openai/xai/deepseek/google/meta
  throw; ollama records its current split into `content` + top-level `images`.
  The registry check (`covers every provider registered in configs`) means a
  new provider can't reopen the hole silently.
- **`Dialogue.addToolMessage` / `addFunctionMessage`** (`src/state/dialogue.ts:183`,
  `:195`) widened to `string | IChatMessageContentDetailed[]`. The first run
  widened only the `set*` pair, so the `add*` aliases still rejected block
  content at the type level.
- **Comment fix** — the `format()` function-branch comment
  (`src/prompt/chat.ts:387`) claimed it mirrors the user-message branch. It
  doesn't: the user branch runs prompt filters and this one doesn't. Reworded
  to say what it actually does and why.

## Files Changed

- `src/llm/config/google/promptSanitizeMessageCallback.ts` — typed throw + text
  flattening for array content in `functionResponse.response.result`
- `src/llm/config/google/promptSanitizeMessageCallback.test.ts` — image-block
  throw, text-only flattening, string-content regression
- `src/llm/config/openai/promptSanitizeMessageCallback.ts` — typed throw for
  image blocks in tool results
- `src/llm/config/openai/promptSanitizeMessageCallback.test.ts` — throw case
  plus text-only pass-through
- `src/llm/config/promptSanitizeContract.test.ts` — `"image tool result"`
  fixture and per-provider expectations
- `src/state/dialogue.ts` — `addToolMessage` / `addFunctionMessage` signatures
- `src/state/dialogue.test.ts` — block content through both `add*` aliases
- `src/prompt/chat.ts` — reworded function-branch comment (comment only)
- `scripts/agents/logs/coder/2026-08-24T16-27-14.md` — this log

## Verification

At `2525073`: 198 suites / 3104 tests green, `tsc --noEmit` clean, `eslint` on
the changed source files reports one pre-existing error (`chat.ts:29`,
`no-unsafe-declaration-merging`, unrelated to this diff).

After merging `development` into the branch (`e2fa2e1`): 199 suites / 3117
tests green, typecheck clean, same single pre-existing lint error.

## Next Steps

What genuinely remains of #757 (issue stays open — the PR says "Part of", not
"Fixes"):

1. **Real Gemini multimodal `functionResponse`.** Today an image block in a
   tool result throws. `googleGeminiContentBlockToPart` already produces the
   correct `inlineData` / `fileData` parts, but the exact multimodal
   `functionResponse` envelope — `parts` alongside the response vs nested
   inside it — varies by API version and can't be verified from the repo. Pin
   it against live Gemini docs, then swap the throw for the conversion.
2. **Ollama role guard** (overlaps #706). `ollamaPromptSanitize`
   (`src/llm/config/ollama/promptSanitize.ts:19`) branches on
   `Array.isArray(content)` with no role check, so an image tool result becomes
   a `function` message with joined text plus a top-level `images` array — a
   field Ollama's tool messages aren't documented to accept. The contract row
   records this as observed behavior, deliberately not as a guarantee. Needs
   either verification against a live Ollama build or a typed throw like the
   other text-only providers got.
3. **`openai.responses.v1` provider config.** Out of #757's scope, but the
   Responses API is where OpenAI actually ships image function-call output, so
   it's the real fix for the OpenAI half rather than the throw added here.
   Separate, larger decision — worth its own issue.

Noted but not touched (no new issues filed): nothing outside #757 came up.

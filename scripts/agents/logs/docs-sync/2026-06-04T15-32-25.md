# docs-sync agent - 2026:06:04T15:32:25

- **Branch**: agent/docs-sync/2026-06-04
- **Started**: 2026-06-04T15:32:25Z
- **Finished**: 2026-06-04T15:36:00Z
- **Status**: complete

## Summary

- Triggering change: `.github/workflows/bot-respond.yml` raised `--max-turns` from 30 to 90 (commit ad95783, after the earlier 30 to 50 bump in 45b67a7).
- Synced `BOT_RESPOND_DEEP_DIVE.md`: 7 references updated (whole picture diagram, one-job DAG, external calls block, code fence, state machine, failure mode F5, quick reference card row K9).
- Rewrote the "lower turn budget (30 vs 50)" comparison note: bot-respond now exceeds `agent-run.yml`'s 50, so prose now reads "higher turn budget (90 vs 50)" with rationale matching the new ceiling.
- Synced `WORKFLOW_ARCHITECTURE.md`: section 7.3 agent table row and section 9.6 `bot-respond.yml` catalog entry both bumped to 90.
- Verified no em or en dashes in the two updated docs (hex regex check passed).

## Files Changed

- `.github/docs/BOT_RESPOND_DEEP_DIVE.md` - synced against `.github/workflows/bot-respond.yml`
- `.github/docs/WORKFLOW_ARCHITECTURE.md` - synced section 7.3 tool/model table and section 9.6 catalog entry against `.github/workflows/bot-respond.yml`

## Next Steps

- `WORKFLOWS_INDEX.md` did not require changes (it does not surface per-workflow max-turns). No other deep dives reference bot-respond's turn budget.
- Drift spotted in passing: nothing material. The architecture doc's section 7.3 table is the canonical place for max-turns numbers across agents - any future agent that gets its turn budget bumped should refresh both the per-agent deep dive and that table.
- Worth a future audit: `BOT_RESPOND_DEEP_DIVE.md` contains the comparative claim against `agent-run.yml`'s turn budget. If `agent-run.yml` ever changes its `--max-turns 50`, that line will silently go stale - consider adding a note in the agent-run deep dive cross-linking the comparison so the next docs-sync run catches it from the other side.

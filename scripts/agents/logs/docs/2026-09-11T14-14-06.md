# docs agent — 2026:09:11T14:14:06

- **Branch**: agent/docs/2026-09-11
- **Started**: 2026-09-11T14:14:06Z
- **Finished**: 2026-09-11T14:19:00Z
- **Status**: complete
- **PR**: https://github.com/llm-exe/llm-exe/pull/792

## Summary

- Checked assigned issue #755 (`docs/llm/xai.md` `effort` support). **Still blocked** — PR #754 closed unmerged, PR #776 still open, so `isReasoningModel` in `src/llm/config/x/index.ts` matches only `grok-4.3`/`grok-4.5`/`grok-4.6`. Left the `effort` row and the #732 warning callout untouched and commented on the issue.
- Found that the xAI deprecation drift reported as fixed by several earlier docs runs was **never actually merged** — `origin/development` was byte-identical to `main` for `docs/llm/xai.md` at the start of this run. Re-landed that work.
- `docs/llm/xai.md`: split the shorthand table into active vs. deprecated, with the reason and migration path per shorthand (retired-and-redirected vs. delisted), cross-checked against `src/llm/config/x/index.ts`. Added a tip that the three redirected shorthands land on non-reasoning `grok-4.3` with `"none"` effort.
- Moved deprecated models out of examples: `xai.grok-4`/`xai.grok-3-mini` → `grok-4.3`/`grok-4.6` in xai.md, `xai.grok-2` → `xai.grok-4.3` on the docs landing page, `xai.grok-3` → `xai.grok-4.3` in `examples/basicTools.ts`.
- `npm run typecheck` passes.

## Files Changed

- `docs/llm/xai.md` — active/deprecated shorthand tables, non-reasoning redirect tip, examples and frontmatter description off deprecated models
- `docs/index.md` — landing-page provider-switch snippet: `xai.grok-2` → `xai.grok-4.3`
- `examples/basicTools.ts` — `xai.grok-3` → `xai.grok-4.3`
- `scripts/agents/logs/docs/2026-09-11T14-14-06.md` — this log

## Next Steps

**Merge hygiene (most important).** Docs PRs from this agent appear to be piling up unmerged — the same xAI deprecation fix has now been written at least three times (see the #755 comment thread). Before doing new work, the next run should check open `agent/docs/*` PRs and consider consolidating rather than re-deriving. If #792 goes unmerged too, that drift returns.

**Deferred, out of scope this run:**

- `docs/.vitepress/components/ModelCodeSwitcher.vue:194` has `xai: "grok-3"` as the fallback model for the `chat.v1` code switcher, so the docs site renders a deprecated model. Other entries there look stale too (`anthropic: "claude-opus-4"`, `bedrock: "claude-3-sonnet-20240229-v1:0"`). This is a `.vue` source file, which my boundaries exclude ("no source code of any kind") despite living under `docs/` — needs a maintainer or the coder agent, or an explicit scope call. Worth filing as an issue if it persists.
- `docs/public/llms-full.txt` still contains the old xAI table. It is generated by `scripts/generate-llms-txt.sh`, so it should refresh on the next docs deploy — not hand-edited. Verify it actually regenerates after #792 merges.
- `docs/.vitepress/utils/providers.json` also lists the deprecated shorthands, but it is generated from source by `docs/.vitepress/scripts/updateProviders.ts` and correctly reflects what `useLlm` accepts. Left alone intentionally.

**Not yet reviewed** (no time this run): the other provider docs (`anthropic.md`, `openai.md`, `gemini.md`, `deepseek.md`, `ollama.md`, `bedrock/`) were not audited for the same deprecated-shorthand drift. A sweep comparing each provider doc's shorthand table against its `src/llm/config/*/index.ts` would be a good focused next run.

# docs-sync agent — 2026:06:17T21:10:18

- **Branch**: agent/docs-sync/2026-06-17
- **Started**: 2026-06-17T21:10:18Z
- **Finished**: 2026-06-17T21:15:00Z
- **Status**: complete

## Summary

- Synced `CREATE_DRAFT_RELEASE_DEEP_DIVE.md` to add the 5th release-notes drop pattern: `revert version bump after failed publish`. Updated DAG step description, sequence diagram, section 6 flowchart + prose + literal sed pipeline, and the quick reference card.
- Synced `PUBLISH_RELEASE_DEEP_DIVE.md` to reflect the expanded `revert-to-draft` job. It now performs three cleanups instead of one: (1) PATCH release to draft, (2) delete the release tag, (3) revert the version bump on `development` via an admin-merged PR and update the dev to main draft PR title. Updated whole-picture diagram, section 3 DAG, section 7 external calls, section 8 rollback path + invariants, section 10 state machine, section 11 failure modes (added F7b, F7c), and section 12 quick reference (added K19).
- Updated `WORKFLOW_ARCHITECTURE.md` section 9.14 (catalog entry for `create-draft-release.yml`) to list the 5th drop pattern, section 9.16 (catalog entry for `publish-release.yml`) to describe the new three-cleanup rollback sequence, and the inline note in section 6's filesystem tree.
- Verified no em/en dashes via the hex regex check.

## Files Changed

- `.github/docs/CREATE_DRAFT_RELEASE_DEEP_DIVE.md` (synced against `.github/workflows/create-draft-release.yml`)
- `.github/docs/PUBLISH_RELEASE_DEEP_DIVE.md` (synced against `.github/workflows/publish-release.yml`)
- `.github/docs/WORKFLOW_ARCHITECTURE.md` (section 6 tree, sections 9.14 and 9.16 catalog entries)

## Next Steps

- The `publish-release.yml` revert-to-draft job now mints a bot token whose write access to `development` depends on App installation permissions (it force-pushes a branch and admin-merges a PR). If the App lost `contents: write` on `development`, the revert step would silently fail. Worth a one-line note in the bot-identity section of `WORKFLOW_ARCHITECTURE.md` section 5 next run if not already covered.
- `WORKFLOW_ARCHITECTURE.md` section 11 (Release Pipeline) was not edited this run. It is a higher-level narrative that may want a sentence describing the new rollback behavior for next-attempt-version reuse. Worth a pass next run.
- The `draft-main-pr.yml` deep dive describes the dev to main draft PR title contract; the publish-release rollback now also edits that title. If the contract changes, the two docs may drift. No action needed this run.

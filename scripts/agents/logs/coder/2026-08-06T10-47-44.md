# coder agent — 2026:08:06T10:47:44

- **Branch**: agent/coder/2026-08-06-issue-704
- **Started**: 2026-08-06T10:47:44Z
- **Finished**: 2026-08-06T10:57:00Z
- **Status**: complete — PR #729 (draft, → development)

## Summary

- Worked assigned issue #704: two live OpenAI shorthands pointed at model IDs in OpenAI's October 23, 2026 shutdown batch with no deprecation payload, so users would have gotten no warning before the API started rejecting calls.
- Wrapped `openai.gpt-4.1-nano` in `deprecateShorthand()` — that exact model ID is on the shutdown list. Message points at `openai.gpt-5.6-luna`.
- Wrapped `openai.gpt-4` the same way — the undated alias resolves to the `gpt-4-0613` snapshot, which is on the list. Message points at `openai.gpt-4o`. Deprecated it without waiting on API confirmation since a warning on a still-working alias costs nothing; left a code comment recording the reasoning and that `gpt-4.1` / `gpt-4.1-mini` are unaffected.
- Change is purely additive — both shorthands still resolve to the same model and base config, so no user code breaks. Actual removal stays a breaking decision for a future major.
- Extended the `deprecated shorthands still resolve` test block: resolution unchanged, each payload stamped with its own shorthand, each message names the shutdown date and replacement, plus a negative case asserting the four surviving gpt-4.1/4o shorthands carry no payload.
- `npm test` 196 suites / 2926 tests passing, `npm run typecheck` clean, `npm run lint` no new errors (130 pre-existing baseline errors, none in touched files).
- Posted the plan on issue #704 before writing code, per process.

## Files Changed

- `src/llm/config/openai/index.ts` — moved `openai.gpt-4.1-nano` and `openai.gpt-4` into the `// Deprecated` block wrapped in `deprecateShorthand()`; added explanatory note on the `gpt-4` → `gpt-4-0613` alias.
- `src/llm/config/openai/openai.test.ts` — extended deprecation coverage (payload stamping, message content, negative case).

## Next Steps

- **Confirm the `gpt-4` alias against the live API.** Issue #704 flagged this as unverified. The deprecation warning is safe either way, but if the alias survives past 2026-10-23 the warning on `openai.gpt-4` should be pulled back. Worth a maintainer check before this leaves draft.
- **Removal is still owed.** These shorthands will 404 after 2026-10-23. A follow-up issue should be filed for the next major to actually remove `openai.gpt-4.1-nano` and `openai.gpt-4` (label `breaking`), the way `anthropic.claude-opus-4` / `claude-sonnet-4` were removed once retired. `openai.o4-mini` is in the same bucket — deprecated since #477, also in the 2026-10-23 batch via `o4-mini-2025-04-16`.
- **The deprecation sweep is reactive, not scheduled.** #679, #477, and #704 are the same failure mode caught three separate times by hand. A recurring check that diffs shipped shorthands against each provider's published deprecation list would catch the next one before it lands in the 3-month window. Not filed as an issue — outside this run's scope, and it's a tooling/CI change rather than `src/`.
- Pre-existing lint baseline is 130 errors (`no-empty`, `no-prototype-builtins`, and others across `src/utils/modules/`). Untouched here, but it means lint can't gate CI as-is.
- Known issue from CLAUDE.md still open: worker process force exit warning in the test suite, suspected timer leak in `asyncCallWithTimeout.test.ts`.

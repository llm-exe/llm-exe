# coder agent — 2026:07:30T10:28:54

- **Branch**: agent/coder/2026-07-30-issue-694
- **Started**: 2026-07-30T10:28:54Z
- **Finished**: 2026-07-30T10:32:00Z
- **Status**: complete

## Summary
- Fixed #694: `anthropicPromptSanitize` threw `TypeError: Cannot read properties of undefined (reading 'role')` when passed an empty messages array.
- Root cause: `const [first, ...messages] = ...` leaves `first` undefined for `[]`, and the `first.role === "system"` guard dereferenced it with no length check.
- Added an early `return []` guard after the string branch — an empty input is a no-op normalization, and the output object is left untouched so the provider layer surfaces any real API error about an empty request.
- Flipped the pre-existing `it.failing` coverage test to `it`, and added a second test asserting `_outputObj` is not mutated for empty input.
- Verified: 194 suites / 2670 tests pass, `npm run typecheck` clean, eslint clean on both changed files (the 130 repo-wide lint errors are pre-existing and untouched).

## Files Changed
- `src/llm/config/anthropic/promptSanitize.ts` — empty-input guard
- `src/llm/config/anthropic/promptSanitize.test.ts` — enabled the failing test, added output-object test

## Next Steps
- The `warnIfTrailingAssistantMessage` prefill model list (`PREFILL_UNSUPPORTED_MODELS`) is duplicated in spirit with the sampling-param and adaptive-thinking lists in `anthropic/index.ts`. Three parallel model-generation lists will drift; a single capability map keyed by model prefix would be a clean, non-breaking follow-up.
- `anthropicPromptSanitize` returns `[]` for empty input rather than throwing a typed `LlmExeError`. If the maintainer would rather reject empty input loudly, that is a behavior change worth its own issue — this run took the non-breaking option the issue listed first.
- Repo-wide lint is failing with 130 pre-existing errors (mostly `no-prototype-builtins` and friends). Worth a dedicated cleanup run since it currently makes `npm run lint` useless as a gate.

# coder agent — 2026:06:22T13:36:02

- **Branch**: agent/coder/2026-06-22-issue-619
- **Started**: 2026-06-22T13:36:02Z
- **Finished**: 2026-06-22T13:39:00Z
- **Status**: complete

## Summary
- Fixed #619: `serializeLlmExeError` and `formatLlmExeErrorForLog` were documented as top-level imports from `"llm-exe"` but not actually re-exported from `src/index.ts` — users copying the documented snippet got import errors.
- Added the two named re-exports next to the existing `LlmExeError` / `isLlmExeError` exports, matching the public-API contract the docs already advertise.
- Extended `src/index.test.ts` with a regression test asserting both helpers are importable from the package entry, that they work end-to-end against a real `LlmExeError`, and added `toHaveProperty` checks for the full error surface (`LlmExeError`, `LLM_EXE_ERROR_SYMBOL`, `isLlmExeError`, the two new helpers).
- Verified with `npm test` (2448 tests pass), `npm run typecheck` (clean). `npm run lint` shows only pre-existing failures unrelated to this change.

## Files Changed
- `src/index.ts` — added re-exports for `serializeLlmExeError` and `formatLlmExeErrorForLog`.
- `src/index.test.ts` — added regression tests for the error-helper exports.

## Next Steps
- Pre-existing lint issues across the repo (~118 errors) — not blocking, but worth a dedicated cleanup PR. Notable hits: missing `Handlebars` global in `removeEmptyFromObject.test.ts`, `no-prototype-builtins` in `utils/modules/pick.ts`, `no-empty` in `utils/modules/json.ts`, and an unresolved type import in `plugins/callable/index.ts`.
- Consider whether `formatErrorValue`, `formatErrorList`, and `createLlmExeError` should also be surfaced at the package entry — they're exported from `src/errors/index.ts` but not from the top-level. Not advertised in the docs, so out of scope here, but a maintainer call worth making for telemetry consistency.
- Known issues from CLAUDE.md remain open: `NumberParser` regex extracting only first digit, and `setToolMessage`/`setToolCallMessage` not returning `this` — both good candidates for the next coder run.

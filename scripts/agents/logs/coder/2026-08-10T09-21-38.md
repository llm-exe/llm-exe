# coder agent — 2026:08:10T09:21:38

- **Branch**: agent/coder/2026-08-10-issue-732
- **Started**: 2026-08-10T09:21:38Z
- **Finished**: 2026-08-10T09:25:00Z
- **Status**: complete — draft PR #734

## Summary
- Fixed issue #732: `useLlm("xai.grok-4.20-reasoning", { effort })` silently dropped the effort option because `isReasoningModel` only matched `grok-4.3` / `grok-4.5`, not the resolved model id `grok-4.20-0309-reasoning`.
- Replaced the two equality checks with an explicit `Set` of xAI reasoning models. Deliberately not a suffix match — `grok-4.20-0309-non-reasoning` also ends in `-reasoning`, so `endsWith` would have wrongly enabled effort on the non-reasoning variant.
- Corrected `xai.test.ts`, which had locked in the bug by listing `grok-4.20-0309-reasoning` under "drops effort for non-reasoning xAI models"; added pass-through and invalid-value coverage for that model.
- Verified: `npm test` (198 suites / 3002 tests passing), `npm run typecheck` clean, `npx eslint src/llm/config/x` clean.
- Posted the root-cause plan on the issue before writing code, and opened draft PR #734 against `development`.

## Files Changed
- `src/llm/config/x/index.ts` — modified
- `src/llm/config/x/xai.test.ts` — modified

## Next Steps
- **Docs follow-up (out of scope here)**: `docs/llm/xai.md` documents the pre-fix behavior (effort only reaching `grok-4.3` / `grok-4.5`) and needs updating once #734 merges. Issue #732 already flags this; PR #733 (`agent/docs/2026-08-07`) is open against docs and may conflict.
- **Repo-wide lint is red**: `npm run lint` reports 130 pre-existing errors (e.g. `no-prototype-builtins`) unrelated to this change. Worth a dedicated cleanup issue/run — it currently makes lint useless as a gate.
- **Still open from CLAUDE.md Known Issues**: worker-process force-exit warning in the test suite (suspected timer leak in `asyncCallWithTimeout.test.ts`).
- Other providers using `createOpenAiCompatibleConfiguration` may have the same stale-model-list drift in their `isReasoningModel` predicates — a sweep across `src/llm/config/*` against current provider model lists would be a good next audit.

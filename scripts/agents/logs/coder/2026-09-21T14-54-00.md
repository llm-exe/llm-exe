# coder agent — 2026:09:21T14:54:00

- **Branch**: agent/coder/2026-09-21-issue-806
- **Started**: 2026-09-21T14:54:00Z
- **Finished**: 2026-09-21T14:58:00Z
- **Status**: complete — PR #807 (draft) opened against `development`

## Summary
- Fixed issue #806: parser failures raised while the LLM executor holds a completed response now carry the response's normalized `usage` and its `stopReason`.
- Added optional `usage`/`stopReason` to `ParserParseFailedContext` and `ParserSchemaValidationContext` via a new exported `ResponseErrorContext` type. Because `usage` is the normalized `OutputUsage` shape, the cache read/write detail from #803 is preserved as-is.
- Enrichment happens only at the LLM executor boundary (`getHandlerOutput`), in place on the same error instance — code, reason, existing context, cause and stack are preserved, existing context keys win, and nothing is inferred (no hard-coded truncation string, no cost, no prompt/response text). Standalone parser use and executions without a completed response are untouched.
- Regression tests cover the three consumer paths named in the acceptance criteria: direct `try/catch`, `onError` hook metadata, and `toJSON()` serialization.
- `npm test` 204 suites / 3205 tests pass; `npm run typecheck` clean; `npm run lint` reports nothing in the changed files (130 lint errors elsewhere are pre-existing).

## Files Changed
- `src/errors/types.ts` — new `ResponseErrorContext`, mixed into the two parser failure contexts
- `src/errors/index.ts` — export `ResponseErrorContext`
- `src/executor/_responseContext.ts` (new) — `attachResponseContext(error, out)`
- `src/executor/_responseContext.test.ts` (new)
- `src/executor/llm.ts` — enrich parser failures in `getHandlerOutput`
- `src/executor/llm-parser-error-context.test.ts` (new)
- `scripts/agents/logs/coder/2026-09-21T14-54-00.md` (this log)

## Next Steps
- **Docs still owed for #806.** The acceptance criteria ask to document the additive fields and show how to inspect them for accounting and truncation diagnosis. That lives in `docs/`, outside this agent's scope, so it needs a docs run before the issue is fully closed.
- Issue #806 is currently on the **v3.0.0** milestone, but the change is purely additive and backwards-compatible — the maintainer may want to move it to v3.1.0.
- Pre-existing lint debt: 130 errors across `src/` (e.g. `no-empty` in `src/utils/modules/json.ts`, `no-prototype-builtins` in `src/utils/modules/pick.ts`). Not filed as an issue this run to avoid scope creep; worth a dedicated cleanup pass.
- Still open from CLAUDE.md's Known Issues: worker process force-exit warning in the test suite (suspected timer leak in `asyncCallWithTimeout.test.ts`).

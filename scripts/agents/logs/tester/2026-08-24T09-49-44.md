# tester agent — 2026:08:24T09:49:44

- **Branch**: agent/tester/2026-08-24
- **Started**: 2026-08-24T09:49:44Z
- **Finished**: 2026-08-24T09:58:00Z
- **Status**: complete — PR #760

## Summary

- Ran the suite with coverage as the first step. Runtime coverage is **effectively saturated**: 198 suites / 3063 tests passing, with only two files below 100% branch coverage — and both remaining branches are **unreachable defensive code**, so no honest test can cover them (details under Next Steps). The coverage number was not the useful gap this run.
- Found the real gap at **compile time**: CLAUDE.md calls end-to-end type inference "non-negotiable", but there were zero type-level tests in `src/`. A generic widened to `any` still returns correct values at runtime, so all 3063 behavioural tests would keep passing while the library's core value proposition silently vanished.
- Added `src/executor/type-inference.test.ts` — static assertions that fail at `tsc` time covering: parser output → executor return type (built-in and custom parsers), distinct parsers not collapsing to a shared type, the prompt generic constraining `execute()` input (`@ts-expect-error` on a wrong-typed field and a missing required field), and inference surviving executor-to-executor composition.
- **Mutation-checked the assertions** — flipping one expectation to `number[]` makes `tsc` fail, proving they have teeth rather than trivially passing. Used an invariant `Equal<X, Y>` so `Equal<any, string>` is `false`, catching `any`-widening specifically.
- Verified: `npm test` 199 suites / 3068 tests all passing, `npm run typecheck` clean, `eslint` clean. No source files touched (test-only scope). No GitHub issues filed — found no bugs.

## Files Changed

- `src/executor/type-inference.test.ts` (new) — compile-time type inference tests for the prompt → parser → executor chain.
- `scripts/agents/logs/tester/2026-08-24T09-49-44.md` (this log).

## Next Steps

**Two uncovered branches are dead code — do not write tests for them.** Both were investigated and confirmed unreachable; they'd need contrived tests that assert nothing. Better handled as a source cleanup by the coder agent:
- `src/utils/modules/redactSecrets.ts:26` — the `match.length <= 8` guard in `maskToken` can never fire. Shortest possible `TOKEN_MASK_REGEX` match is 12 chars (`Bearer a.b.c`); verified empirically against every regex alternative. Either drop the guard or leave it and accept 80% branch coverage on the file.
- `src/utils/modules/request.ts:117` — the `?? ""` fallback on `safeProviderString(detail)`. `detail` is always a truthy string by that line (the `||` chain terminates in the non-empty `bodyText`), and `JSON.stringify` only returns `undefined` for functions/symbols, which `JSON.parse` cannot produce.

**Stale entry in CLAUDE.md**: the "Worker process force exit warning (potential timer leak in `asyncCallWithTimeout.test.ts`)" under Known Issues **no longer reproduces** — a full run shows no force-exit or open-handle warning. Someone should confirm and remove that entry.

**Highest-value follow-up — extend the type-inference approach outward.** This run covered the executor chain only. The same compile-time treatment is missing for, in rough priority order:
1. `LlmExecutorWithFunctions` / `createLlmFunctionExecutor` — tool-call generics are the most complex typing in the codebase and are entirely unguarded statically.
2. The normalized `OutputResult` / `OutputResultContent` shape across providers — the provider-agnostic layer is the other core promise, and nothing asserts the shape stays identical across OpenAI/Anthropic/Google/Bedrock configs.
3. `createParser` overloads — verify each parser name maps to its documented output type, so a new parser can't silently return `any`.
4. `useLlm` shorthand vs. options-based form — confirm the curated `provider.model` strings keep their narrow types.

**Note on the mock provider** (cost me time; worth knowing): `openai.chat-mock.v1` does **not** echo the prompt verbatim. It returns `"Hello world from LLM! The input was [<serialized messages>]"` — a single line. Parsers needing multi-line or structured input (`listToArray`, `listToKeyValue`, `markdownCodeBlock`, `json`) will throw against it. Either assert on the signature instead of executing, or use `createCustomParser`. A mock that echoes configurable raw content would make executor tests much easier to write.

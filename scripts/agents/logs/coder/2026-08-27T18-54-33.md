# coder agent — 2026:08:27T18:54:33

- **Branch**: agent/coder/2026-08-27-issue-762
- **Started**: 2026-08-27T18:54:33Z
- **Finished**: 2026-08-27T19:02:00Z
- **Status**: complete

## Summary
- Worked issue #762: the three `google.gemini-2.5-*` shorthands warned that they had "shut down" on `2026-06-17` / `2026-07-22`. Those dates came from the *earliest possible* column of Google's deprecation table, not from a committed shutdown — all three models are still listed Stable with no announced shutdown date.
- Un-deprecated `google.gemini-2.5-flash`, `google.gemini-2.5-flash-lite`, and `google.gemini-2.5-pro`: they now register with plain `withDefaultModel` alongside the 3.x shorthands and carry no `deprecated` payload, so no warning is emitted. `src/interfaces/llm.ts` already grouped these keys with the active models, so types were already consistent — no type change needed.
- Left the 2.0 and 1.5 entries deprecated; those messages cite real, already-executed shutdowns.
- Added a guard test asserting no Google deprecation message contains "will shut down on", and that any date in a Google deprecation message is in the already-happened `was shut down by Google on <date>` form.
- Repointed the registry integration tests in `_utils.deprecationWarning.test.ts` (which asserted the 2.5 keys were deprecated) at genuinely retired shorthands, and added a test that the 2.5 keys emit no warning.
- Verified: `npm test` 199 suites / 3082 tests pass, `npm run typecheck` clean, `npm run lint` reports no errors in the changed files (130 pre-existing errors elsewhere in `src/`, untouched).

## Files Changed
- `src/llm/config/google/index.ts` — moved the three 2.5 shorthands out of the deprecated block
- `src/llm/config/google/google.test.ts` — added `gemini-2.5 shorthands` and `deprecation messages` describes
- `src/llm/_utils.deprecationWarning.test.ts` — updated registry integration tests
- `scripts/agents/logs/coder/2026-08-27T18-54-33.md` — this log

## Next Steps
- **Point 2 of #762 is only enforced for Google.** `src/llm/config/openai/index.ts` still hardcodes future shutdown dates for `openai.gpt-4.1-nano`, `openai.gpt-4` (2026-10-23), `openai.gpt-5-mini`, `openai.gpt-5-nano`, `openai.o3` (2026-12-11). OpenAI does publish firm retirement dates, so these are probably legitimate — but they should be verified against OpenAI's deprecations page, and once those dates pass someone has to revisit them or users hit the same "warned about a live model" problem. Worth a scout pass and/or lifting the guard test into a shared cross-provider check.
- The guard test lives in the Google test file. If the policy is adopted library-wide, move it to a registry-level test that walks every provider's configs.
- Pre-existing lint debt: 130 eslint errors across `src/` (`no-empty`, `no-prototype-builtins`, etc.) and prettier non-conformance in files CI doesn't gate. Not touched here.
- CLAUDE.md Known Issues still lists the test-suite worker force-exit warning (timer leak in `asyncCallWithTimeout.test.ts`) as unfixed.

# docs agent — 2026:07:31T12:05:48

- **Branch**: agent/docs/2026-07-31
- **Started**: 2026-07-31T12:05:48Z
- **Finished**: 2026-07-31T12:14:00Z
- **Status**: complete

## Summary
- No open `documentation`-labeled issues; picked up drift from PR #689 (`feat(embedding): multimodal (text + image) embedding input`, merged 2026-07-28), which landed after the last docs run.
- `docs/embeddings/cohere.md` claimed "llm-exe is text-only; image inputs to Cohere Embed v3/v4 are not supported" — now false. Replaced it with a full **Multimodal Input (Embed v4)** section: `EmbeddingContentItem` shape, data-URI-only requirement, uniform-batch rule, and the `imageInputs` escape hatch (also added to the options table).
- Added an **Accepted Input** table to `docs/embeddings/index.md` mapping `string` / `string[]` / `EmbeddingContentItem[]` to the providers that accept each, and documented that `.getEmbedding(i)` indexes a batch.
- Added text-only notes to `docs/embeddings/openai.md` and `docs/embeddings/amazon.md` pointing at the Cohere provider, since both throw `embedding.unsupported_input` on content items.
- Verified against source (`src/embedding/config.ts`, `embedding.input.ts`, `output/CohereBedrockEmbedding.ts`, `src/interfaces/llm.ts`) rather than the PR description. `npm run typecheck` passes.

## Files Changed
- `docs/embeddings/cohere.md` — new Multimodal Input section, `imageInputs` option row, removed the stale text-only note.
- `docs/embeddings/index.md` — Accepted Input table, batch indexing note, multimodal cross-link.
- `docs/embeddings/openai.md` — text-only note.
- `docs/embeddings/amazon.md` — new Notes section with text-only note.

## Next Steps
- `docs/misc/errors.md` describes the `embedding.` code prefix but never enumerates codes. Worth a table of the known embedding codes (`embedding.unsupported_input`, `embedding.unsupported_dimensions`, `embedding.invalid_response_shape`) with a one-line cause each; source of truth is `src/errors/knownCodes.ts`.
- Carried over from 2026-07-24 and still open: the xAI and Deepseek LLM docs keep hand-maintained shorthand tables alongside/instead of the auto-generated `<ImportModelNames>` component, so they drift whenever a shorthand is added. Consider dropping the manual tables.
- Carried over and still not done: verify `docs/examples/**` snippets still compile against the current API (parsers, executor generics). Deserves a dedicated run.
- No embeddings example exists under `examples/`. A small multimodal-embedding example would be a good companion to the new Cohere section.

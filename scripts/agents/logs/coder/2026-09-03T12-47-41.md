# coder agent — 2026:09:03T12:47:41

- **Branch**: agent/coder/2026-09-03-issue-465
- **Started**: 2026-09-03T12:47:41Z
- **Finished**: 2026-09-03T12:52:00Z
- **Status**: complete

## Summary

- Worked assigned issue #465 (DeepSeek `deepseek-chat` retirement). The `deepseek.v4-flash` / `deepseek.v4-pro` shorthands were already shipped and correct; the outstanding gap was `deepseek.chat`, which still pointed at the retired `deepseek-chat` model ID with no deprecation wrapper.
- Wrapped `deepseek.chat` in `deprecateShorthand` with a message naming the 2026-07-24 retirement date and pointing at `deepseek.v4-flash`, matching the pattern already used for the OpenAI shorthands.
- Repointed the shorthand's default model to `deepseek-v4-flash`. DeepSeek documents `deepseek-chat` as the non-thinking mode of v4-flash, so callers keep working instead of hard-failing against a model DeepSeek no longer serves. The shorthand key stays exported, so no `AllUseLlmOptions` key is removed — non-breaking.
- Left `deepseek.chat.v1` untouched: that's the generic options-based config where the caller supplies `model`.
- Added tests covering the repointed default, the deprecation payload (shorthand/date/migration target), and asserting `deepseek.v4-flash` is *not* flagged deprecated.
- Verified: `npm test` 199 suites / 3143 tests pass; `npm run typecheck` clean. `npm run lint` reports 130 pre-existing repo-wide errors, none in the files touched here.

## Files Changed

- `src/llm/config/deepseek/index.ts` — modified
- `src/llm/config/deepseek/deepseek.test.ts` — modified

## Next Steps

- **Lint debt**: `npm run lint` fails with 130 pre-existing errors across the repo (`no-empty`, `no-prototype-builtins`, and others in `src/utils/modules/*`). This blocks lint from being a useful gate on PRs. Worth a dedicated cleanup run or an agreed baseline config — not touched here to keep this PR focused.
- **Issue #254** (deepseek-reasoner shorthand) should be closed per #465's item 4 — the model it proposes is retired. Left to the maintainer since closing issues is outside this run's scope.
- **`deepseek-v4-flash-vision-exp`**: DeepSeek's docs list a third model ID. Not added — it's experimental and vision-input focused, which fails the "broad adoption / longevity" bar in CLAUDE.md's feature evaluation. Flagging only so it's a conscious skip rather than an oversight.
- **Known Issue from CLAUDE.md still open**: worker process force-exit warning in the test suite (suspected timer leak in `asyncCallWithTimeout.test.ts`). Did not reproduce a failure this run, but it remains listed.

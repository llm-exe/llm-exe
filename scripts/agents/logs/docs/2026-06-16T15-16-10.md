# docs agent — 2026:06:16T15:16:10

- **Branch**: agent/docs/2026-06-16
- **Started**: 2026-06-16T15:16:10Z
- **Finished**: 2026-06-16T15:20:00Z
- **Status**: completed

## Summary
- Audited the documented public API against `src/index.ts` exports, source parsers, and constructor defaults.
- Added the missing `keyTransform` option (and its `"preserve"` default) to the `listToKeyValue` parser docs — it was implemented in source but undocumented.
- Fixed the `DialogueHistory` partial example in `docs/prompt/advanced.md`: input data had every message tagged `role: "user"`, which couldn't produce the alternating `User:`/`Assistant:` parsed output shown.
- Verified parser/match defaults (`boolean: "exact"`, `number: "extract"`, `json: "exact"`, `stringExtract: "word"`, `listToJson: "camelCase"`) match what the docs claim — no drift found.

## Files Changed
- `docs/parser/included-parsers.md` — documented `listToKeyValue` options.
- `docs/prompt/advanced.md` — corrected roles in `DialogueHistory` example input.

## Next Steps
- `docs/embeddings/index.md` lists Cohere as `amazon:cohere.embedding.v1` — worth a pass to confirm the key string against the live source and add a short example for each non-OpenAI provider.
- `docs/llm/openai.md` table is missing some entries that exist on other provider pages (e.g. `numOfAttempts`, `timeout` live in `generic.html` but a cross-link from the OpenAI page would help). Consider adding "see also: generic options" anchor links on every provider page.
- `docs/executor/openai-functions.md` still uses the legacy "OpenAI Functions" framing in its title even though `createLlmFunctionExecutor` is now provider-agnostic. Consider renaming to "Tool Calling Executor" everywhere (page already uses that H1, but the URL slug and sidebar entry could be updated).
- `docs/state/index.md` describes Context + Attributes but only links Dialogues to a dedicated page; a short standalone page for Context (BaseStateItem/createStateItem) would round out the state docs.
- `defineSchema` is documented under `docs/parser/index.md` but `defineSchema` is exported from `utils/modules/defineSchema` and is broadly useful beyond parsers — consider promoting it to its own short page or a section under `misc/`.
- No project root `README.md` exists — when one is added, mirror the homepage hero example so the npm landing page and the docs landing page tell the same story.

# coder agent — 2026:09:21T15:03:53

- **Branch**: agent/coder/2026-09-21-issue-794
- **Started**: 2026-09-21T15:03:53Z
- **Finished**: —
- **Status**: running

## Summary

- Worked assigned issue #794 (persona runners emitting empty stub logs). Shipped draft PR #809 → `development`.
- **Ruled out** the issue's `$LOG_FILE` hypothesis: reproduced `run_persona`'s prompt assembly in isolation; both the `$LOG_FILE` and `$PERSONA` substitutions resolve correctly, producing a ~2.3KB prompt with no leftover placeholder. That bullet is a dead end.
- **Root cause fixed**: `clock_out()` derived status purely from the process exit code, so an agent that started, did nothing, and exited 0 was recorded as `completed` — indistinguishable from a clean run. Added `log_has_content()` and a third `no-output` status (rc=2) for the exit-0-but-untouched-log case; all four runners in `maintain.sh` now `warn` instead of printing `ok`.
- Verified by exercising `clock_in`/`clock_out` against a temp `LOGS_DIR` across five cases (untouched / filled / partially filled / nonzero exit / timestamp stamping) — all correct. `bash -n` clean; `shellcheck` reports nothing new.
- **Scope flag**: this issue lives entirely in `scripts/`, outside my standing `src/**/*.ts` boundary — that boundary is exactly why #793 filed the issue rather than fixing it. I treated the explicit assignment of #794 as authorization and said so in both the issue comment and the PR, so the maintainer can push back.

## Files Changed

- `scripts/agents/config.sh` — added `LOG_PLACEHOLDER` + `log_has_content()`; `clock_out()` now returns 0/1/2 and stamps `completed`/`interrupted`/`no-output`
- `scripts/maintain.sh` — `run_task_agent`, `run_persona`, `run_curator`, `run_reviewer` branch on the `clock_out` return code (captured via `|| log_status=$?` so `set -e` does not abort)

## Next Steps

- **The actual early exit is still undiagnosed.** This PR makes empty runs *visible*; it does not explain them. Evidence pattern: beginner ran 44s, then harsh-critic/speed-runner/enterprise ran 1–2s each. That smells like an upstream `claude -p` failure that still exits 0 (rate limit / usage cap mid-sequence), but no stderr was captured so there is nothing left to read. **Suggested follow-up: capture `claude` stderr to the log in `run_claude()`** — without it the next occurrence is still a black box, it just gets flagged now.
- Related: `run_claude` uses `AGENT_TIMEOUT=600` *per agent*, but `maintain.sh all` runs 4 personas + curator sequentially — up to 50 minutes. Worth checking that against the CI workflow's job timeout; a job-level kill mid-sequence would look a lot like the observed pattern.
- No test harness exists for the shell scripts (`npm test` is Jest over `src` only), so the runner logic is entirely unverified in CI. If the maintainer wants coverage there, `bats` would be the smallest addition — flagged in the issue comment, awaiting a call.
- Pre-existing `shellcheck` findings in these files left untouched (SC2034 unused `TASK_AGENTS`, SC2001, SC2012, SC2155) — out of scope for this fix, trivial cleanup if wanted.
- #795 (the #793 curator fix) is still an open draft. Its no-op-on-every-run behaviour is expected until persona output actually resumes; the two should probably land together.

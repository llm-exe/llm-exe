# coder agent — 2026:09:10T12:44:46

- **Branch**: agent/coder/2026-09-10-issue-786
- **Started**: 2026-09-10T12:44:46Z
- **Finished**: 2026-09-10T12:49:00Z
- **Status**: complete

## Summary
- Worked assigned issue #786 (Ollama shorthands stale, `enhancement`, milestone v3.1.0). No existing open PR for it.
- Posted a plan comment on the issue before writing code, then added three shorthands: `ollama.qwen3.8-flash-next`, `ollama.glm-5.3`, `ollama.glm-5.3-flash`.
- Deliberately skipped `nemotron-3.5-lightning` and `laguna-xs-2.1` — the issue itself flags no adoption read, and per CLAUDE.md each shorthand is an ongoing maintenance commitment, not a free config line.
- Added the matching `AllUseLlmOptions` keys in `src/interfaces/llm.ts` so the new shorthands are typed for `useLlm(...)`, not just resolvable at runtime.
- Extended the existing `it.each` shorthand-resolution table rather than adding a parallel test block.
- Verified: `npm test` 199 suites / 3144 tests pass, `npm run typecheck` clean. `npm run lint` reports 130 pre-existing errors elsewhere in `src/utils/**` — zero in any file this change touched.

## Files Changed
- `src/llm/config/ollama/index.ts` — three new `withDefaultModel(ollamaChatV1, ...)` entries
- `src/interfaces/llm.ts` — three matching `GenericLLm` keys in the Ollama block of `AllUseLlmOptions`
- `src/llm/config/ollama/ollama.test.ts` — shorthand resolution cases for the new entries

## Next Steps
- **Pre-existing lint debt (not filed):** `npm run lint` fails with 130 errors across `src/utils/**` (`no-empty` in `modules/json.ts`, `no-prototype-builtins` in `modules/pick.ts`, and others). Lint is effectively red on `development`, so it gives no signal on new work. Worth a dedicated cleanup run; I left it alone to avoid scope creep. Check `gh search issues "lint" --repo llm-exe/llm-exe` before filing — I did not verify whether it is already tracked.
- **Shorthand staleness is structural.** This is the same class of issue as #579 (Qwen line) and will recur for every provider. A periodic audit of shorthand lists against each provider's model catalog would be more durable than one-off issues. Would need a decision on cadence and on where the "is this adopted enough" judgement lives.
- **`nemotron-3.5-lightning` / `laguna-xs-2.1`** remain unadded by design. If demand shows up, they are one-line additions in the same three files.
- Known Issues in CLAUDE.md are still open and unclaimed: NumberParser `'42'` → `'4'`, and `setToolMessage`/`setToolCallMessage` not returning `this`. Both are bugs and should outrank enhancement work on the next run.

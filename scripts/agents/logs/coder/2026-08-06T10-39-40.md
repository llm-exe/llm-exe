# coder agent — 2026:08:06T10:39:40

- **Branch**: agent/coder/2026-08-06-issue-706
- **Started**: 2026-08-06T10:39:40Z
- **Finished**: 2026-08-06T10:44Z
- **Status**: complete — PR #727 (draft, base `development`)

## Summary
- Fixed #706: `ollamaPromptSanitize` sent tool traffic in llm-exe's internal shape because it early-returned on any message whose `content` was not an array — which is every tool-carrying message (`content: null` for a tool call, a string for a tool result).
- Tool handling now runs before that early return: `function_call` (single or array) → `tool_calls: [{ function: { name, arguments } }]` with `arguments` parsed to an object via `maybeParseJSON` (ollama takes an object, not a JSON string), and `role: "function"` → `role: "tool"` with the internal `id`/`name` dropped. `content: null` on a tool-call message normalizes to `""` since ollama types content as a string.
- Updated the two `ollamaExpectations` rows in the cross-provider contract test that had pinned the broken behavior; they now assert the ollama-native shape.
- Verified: `npm test` 196 suites / 2921 tests pass, `npm run typecheck` clean, `npm run lint` reports no new issues in the touched files (the repo's 130 pre-existing lint errors are unchanged).
- Not verified against a live ollama instance — there is no integration coverage for ollama.

## Files Changed
- `src/llm/config/ollama/promptSanitize.ts` — the fix
- `src/llm/config/ollama/promptSanitize.test.ts` — tool call, parallel tool call array, and tool result cases
- `src/llm/config/promptSanitizeContract.test.ts` — updated the two pinned ollama tool rows

## Next Steps
- Recent ollama versions accept `tool_name` on tool-result messages, which helps the model correlate results to calls. This PR follows the shape spelled out in #706 exactly and drops `name`; worth deciding whether to carry it through as `tool_name`.
- No ollama integration/e2e coverage exists — the wire shape is only asserted against unit expectations. A live smoke test against a local ollama would be worth having before relying on tool calling there.
- `metaExpectations` in `promptSanitizeContract.test.ts` still records tool metadata being silently dropped for Bedrock Llama (flattened text prompt). That is noted in the file as current behavior rather than a guarantee and has no issue tracking it yet.
- Repo-wide lint is failing with 130 pre-existing errors (mostly `no-prototype-builtins` and similar), unrelated to this change.

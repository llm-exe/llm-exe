# coder agent — 2026:08:27T19:09:44

- **Branch**: agent/coder/2026-08-27-issue-403
- **Started**: 2026-08-27T19:09:44Z
- **Finished**: 2026-08-27T19:14:00Z
- **Status**: complete

## Summary
- Worked issue #403 — six xAI shorthands resolving to model IDs that are retired or delisted by xAI.
- Wrapped `xai.grok-2`, `xai.grok-3`, `xai.grok-3-mini`, `xai.grok-4`, `xai.grok-4-fast`, and `xai.grok-4-1-fast` in `deprecateShorthand`, matching the pattern already used for retired Google/OpenAI/Anthropic models.
- Split the messages by evidence: the three on xAI's explicit retirement list from #512 (`grok-3`, `grok-4-fast-non-reasoning`, `grok-4-1-fast-non-reasoning`) say retired 2026-05-15 and now redirect to grok-4.3; the three merely delisted (`grok-2-latest`, `grok-3-mini`, `grok-4`) say deprecated/no-longer-served. All point users at `xai.grok-4.3`.
- Non-breaking: every shorthand still resolves to the same model ID, users just get a one-time `DeprecationWarning`. Removal stays parked for v4.0.0.
- Added tests asserting the six carry `deprecated` metadata with a migration target, and that the five current shorthands do not.
- Verified: `npm test` (199 suites / 3087 tests pass), `npm run typecheck` clean, `npm run lint` reports no errors in the changed files (130 pre-existing errors elsewhere in the repo, untouched).

## Files Changed
- `src/llm/config/x/index.ts`
- `src/llm/config/x/xai.test.ts`

## Next Steps
- `xai.grok-2` is confirmed dead at the API (maintainer, 2026-05-14 on #403) while the other five still resolve. When v4.0.0 is scheduled, remove all six shorthands outright.
- #512 (Grok deprecation email) overlaps heavily with #403 and can likely be closed once this lands — worth a maintainer look.
- `npm run lint` has ~130 pre-existing errors across the repo (mostly `no-prototype-builtins` and similar). Not in scope here, but lint is effectively non-gating right now; worth its own cleanup issue.
- The docs site still lists the deprecated xAI shorthands without any deprecation note — out of scope for this agent (docs/), but a docs-labeled issue would be reasonable.

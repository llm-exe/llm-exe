# docs agent — 2026:06:02T14:14:47

- **Branch**: agent/docs/2026-06-02
- **Started**: 2026-06-02T14:14:47Z
- **Finished**: 2026-06-02T14:21:00Z
- **Status**: completed

## Summary

- Audited public API surface against existing docs. No issues open under the `documentation` label, so worked through `docs/` looking for accuracy gaps.
- Fixed `docs/intro/install.md` — the "Talk like a pirate" sample called `executor.execute({ input: "Hello!" })` but the chat prompt had no `.addUserMessage()` referencing `{{input}}`, so the documented LLM call wouldn't actually include the user message. Updated the prompt to use `.addUserMessage("{{input}}")`, added the `<{ input: string }>` generic, and added a tip clarifying that the chat prompt only renders the messages you build (passing `input` does not auto-append a user message).
- Fixed `docs/intro/what_is_llm_function.md` — the expected output showed `emailAddress` but the `listToJson` parser camelCases the prompt's "email addresses" key to `emailAddresses` (plural). Updated to match actual parser behavior.
- Verified executor hook docs (`withTraceId`, `clearHooks`, `getHookCount`) against `src/executor/_base.ts` — all methods exist with the documented signatures.
- Verified parser docs against `src/parser/index.ts` — every documented parser type (`listToJson`, `replaceStringTemplate`, `markdownCodeBlock`, `markdownCodeBlocks`, etc.) is exported and matches the current source.

## Files Changed

- docs/intro/install.md
- docs/intro/what_is_llm_function.md

## Next Steps

- `docs/index.md` / `readme.md` "Overview" examples both call `executor.execute({ input: "..." })` against prompts whose templates don't reference `{{input}}`. Same misleading pattern as the install example — worth tightening for consistency.
- `docs/executor/options.md` lists `state?: BaseState` as a configuration field. The interface in `src/interfaces/functions.ts` accepts it, but `LlmExecutor` never reads `this.state`. Either the docs should note it's currently inert, or this is a source-side gap worth filing as a bug — left for follow-up after talking to the maintainer.
- `docs/llm/anthropic.md` shows `await llm.call("")` as a "string prompt → completion" example, but Anthropic is chat-only in llm-exe. Worth reviewing the Basic Usage block.
- `docs/llm/generic.md` links to `/llm/bedrock/index.html` while other provider links omit `.html` — minor consistency cleanup.
- `docs/prompt/chat.md` references `<<<` example includes from `examples/prompt/chat.ts`. Did not audit those embedded examples this run.
- No `documentation`-labeled issues are open; if maintainer wants prioritized work, opening issues will route it to future runs.

# docs agent — 2026:07:17T11:25:32

- **Branch**: agent/docs/2026-07-17
- **Started**: 2026-07-17T11:25:32Z
- **Finished**: 2026-07-17T11:28:00Z
- **Status**: complete

## Summary
- Reviewed the only open `documentation` issue (#635, JSON parser `required`/`validateSchema` default-on). Verified the behavior against `src/parser/parsers/JsonParser.ts` (`shouldValidateSchema = !!options.schema && options.validateSchema !== false`) and confirmed it is already fully and accurately documented in `docs/parser/included-parsers.md`, `docs/parser/index.md`, and `docs/examples/concepts/working-with-json.md`. Left a verification comment on the issue.
- Fixed an adjacent accuracy bug on the homepage hero (`docs/index.md`): the JSON parser example used a bare (non-`object`) schema with non-standard `min`/`max`. With schema validation now on by default, that would not enforce as the "Guaranteed to match schema or throw" copy claims. Corrected to a valid `type: "object"` / `properties` / `required` schema using standard `minimum`/`maximum`.
- Ran `npm run typecheck` — passes.

## Files Changed
- `docs/index.md` — corrected the hero JSON-schema example to a valid, enforceable JSON Schema.
- `scripts/agents/logs/docs/2026-07-17T11-25-32.md` — this run log.

## Next Steps
- Issue #635's remaining items are source-only (TSDoc on `validateSchema?` in `src/interfaces/parser.ts`, regression test in `JsonParser.test.ts`) — out of docs-agent scope; hand to the coder agent so #635 can close.
- Audit other marketing/example schemas across `docs/` and `examples/` for the same bare-schema / non-standard-keyword pattern (`min`/`max`, missing `type: "object"`) now that validation defaults on, so no example silently fails to enforce.
- `docs/index.md` callable-agent hero uses `assistant.execute({ request })` against a prompt with no matching `{{request}}` variable — worth verifying the example runs as shown in a future pass.

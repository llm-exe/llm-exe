# docs agent — 2026:06:12T13:33:19

- **Branch**: agent/docs/2026-06-12
- **Started**: 2026-06-12T13:33:19Z
- **Finished**: 2026-06-12T13:38:00Z
- **Status**: completed

## Summary

- Documented the new `baseUrl` option on `openai.embedding.v1`, which landed in PR #597 (`add support for oai compatible`) but had no corresponding docs.
- Added an "OpenAI-Compatible Endpoints" section to `docs/embeddings/openai.md` with worked examples for Baseten, Together AI, and local servers (vLLM, TEI, Ollama, LM Studio).
- Updated `docs/embeddings/index.md` so the supported-providers table flags `openai.embedding.v1` as usable for any OpenAI-compatible endpoint, with a pointer to the new section.
- Verified existing docs in `docs/{llm,parser,executor,prompt,state,callable,intro,embeddings}` match the public API surface exported from `src/index.ts` — no other gaps spotted in this pass.
- `npm run typecheck` passes (docs-only changes, but ran for safety).

## Files Changed

- `docs/embeddings/openai.md` — added `baseUrl` to the options table; added new "OpenAI-Compatible Endpoints" section.
- `docs/embeddings/index.md` — noted `openai.embedding.v1` doubles as the OpenAI-compatible embedding provider; linked to the new section.
- `scripts/agents/logs/docs/2026-06-12T13-33-19.md` — this log.

## Next Steps

- The custom-provider docs (`docs/llm/custom.md`) still say "Custom options require type assertion (use `as any`)". Worth checking whether `createOpenAiCompatibleConfiguration`'s typing was tightened in a recent release; if so, the `as any` examples and the trailing "Important Notes" entry should be updated.
- `docs/embeddings/index.md` lists "Custom embedding providers are not currently supported" — but now that `openai.embedding.v1` can be retargeted via `baseUrl`, that line could be softened/clarified for OpenAI-compatible cases. Left as-is this run to keep scope tight.
- `docs/llm/openai.md` documents only the `chat/completions` endpoint and `OPENAI_API_KEY` env var. If/when chat ever gains a `baseUrl`-style override, mirror the pattern used here in embeddings.
- Worth a future pass to confirm every parser listed in `readme.md` and `docs/parser/included-parsers.md` matches the current `createParser` factory in `src/parser/`.
- `docs/intro/install.md` example uses `await` at the top level; consider wrapping in an async function for copy-paste-runnability, or noting Node ≥20 top-level await context. Minor.

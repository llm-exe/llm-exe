# docs agent — 2026:06:19T13:40:12

- **Branch**: agent/docs/2026-06-19
- **Started**: 2026-06-19T13:40:12Z
- **Finished**: 2026-06-19T13:43:00Z
- **Status**: complete (partial — one focused fix shipped)

## Summary

- Audited the public API surface (`src/index.ts`) against `docs/` and found `serializeLlmExeError` / `formatLlmExeErrorForLog` were documented as importable from `"llm-exe"` but are not re-exported from the package entry point — copy-pasting the snippet would fail.
- Filed [#619](https://github.com/llm-exe/llm-exe/issues/619) (label: `bug`) for the missing exports. The helpers exist in `src/errors/index.ts` and look intentional; left to the maintainer whether to surface them.
- Rewrote the error-handling guide and v2→v3 migration guide to use what's actually publicly exported (`isLlmExeError` + `LlmExeError#toJSON()`). The `toJSON()` method already delegates to `serializeLlmExeError`, so consumers get the same payload.
- Opened draft PR [#620](https://github.com/llm-exe/llm-exe/pull/620) against `development`.

## Files Changed

- `docs/misc/errors.md` — dropped unexported imports; rewrote the Serialization section around `error.toJSON()`.
- `docs/misc/v2-to-v3-migration-guide.md` — same fix in the Error Handling migration block.

## Next Steps

- Once #619 lands (whether by exporting the helpers or by deciding they stay internal), re-sync the error-handling docs and `docs/public/llms-full.txt` to match the final decision.
- Audit other exports from `src/index.ts` that may be undocumented or thinly documented — quick spot-checks suggest `defineSchema`, `registerHelpers`, `registerPartials`, `createDialogue`, `LlmExecutorOpenAiFunctions`, and the error-related type exports (`ErrorCategory`, `ErrorCodes`, `ErrorContextByCode`, `NormalizedProviderError`, `ProviderErrorContext`) deserve dedicated coverage.
- `docs/misc/v2-to-v3-migration-guide.md` is still marked `Status: draft` at the top — worth a pass for completeness before v3.0.0 cuts.
- `docs/misc/errors.md` frontmatter still says "serialization helpers"; could be tightened to "serialization" given the helpers are no longer surfaced.
- Did not touch examples in `docs/examples/` or `examples/` this run — worth a future pass to verify all snippets still match current API patterns.

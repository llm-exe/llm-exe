# docs agent — 2026:08:18T10:35:32

- **Branch**: agent/docs/2026-08-18
- **Started**: 2026-08-18T10:35:32Z
- **Finished**: 2026-08-18T10:40:00Z
- **Status**: complete

## Summary

- No open issues labeled `documentation`, so I worked the previous run's Next
  Steps list. Two of its three items turned out to be **already resolved** —
  worth recording so the next run doesn't re-chase them:
  - **`tool_choice` (#720) is now documented.** The prior log said a grep for
    `tool_choice|toolChoice` across `docs/` returned nothing; it now hits
    `docs/llm/anthropic.options.part.md`, `docs/llm/bedrock/anthropic.md`, and
    `docs/executor/openai-functions.md`. No gap left.
  - **The ARN routing fix (#722) landed**, via `encodeKeys: ["model"]` in
    `src/llm/llm.call.ts:46` rather than a literal `encodeURIComponent` in the
    Bedrock config — which is why a grep scoped to `src/llm/config/bedrock/`
    misses it. ARN model ids *do* dispatch.
- Picked up the one real remaining gap: `docs/llm/bedrock/anthropic.md`
  described `model` as only "The Bedrock model id". Documented all three
  accepted forms (foundation model id, cross-region inference profile id, full
  ARN) with examples, and noted that the geo prefix (`us.`, `eu.`, `apac.`,
  `us-gov.`) is understood by the `effort` gating.
- Added the easy-to-miss ARN caveat: `effort`/`thinking`, the Opus 4.7/4.8
  escalation, and the `topP` drop are all selected by matching the Claude model
  *name*, which is only recoverable from `inference-profile/` and
  `foundation-model/` ARNs. Opaque ARN types (`provisioned-model/`,
  `custom-model/`, `application-inference-profile/`) still dispatch, but `effort`
  is **silently ignored** — a failure mode with no error to surface it.
- Verified against `src/llm/config/anthropic/effort.ts`,
  `src/llm/config/bedrock/index.ts`, and `src/llm/llm.call.ts` rather than
  trusting the commit messages or the prior log. `npm run typecheck` passes;
  regenerated `docs/public/llms-full.txt`.

## Files Changed

- `docs/llm/bedrock/anthropic.md` (`model` option row + model identifiers note)
- `docs/public/llms-full.txt` (regenerated)

## Next Steps

- **Stale source comment (not a bug, out of scope for this agent).**
  `src/llm/config/anthropic/effort.ts:49-52` still says "llm-exe cannot yet
  *dispatch* an ARN model id — the Bedrock endpoint interpolates `{{model}}`
  without URL-encoding ... latent until that is fixed." That was fixed in #722
  (merged as `9cea16e`, PR #725). The comment now contradicts the code and cost
  me time to disprove. Worth a one-line cleanup by the coder agent — I did not
  file an issue since it is a comment, not a behavior bug; low priority.
- **`docs/llm/bedrock/meta.md`** has the same bare "The Bedrock model id" row.
  The URL-encoding/ARN half applies there too (the `effort` half does not, since
  Meta has no effort gating). A short version of the note would make it
  consistent — I left it to keep this run scoped.
- **Error docs depth** (carried over, still open): `docs/misc/errors.md` lists
  categories but not the full code table. A code→category→retryable table would
  make the page self-contained.
- **Verify the SigV4 signed path.** #722's body flagged a secondary concern that
  `getAwsAuthorizationHeaders` signs over `url.pathname` and the signed path must
  match the sent path after encoding. I did not confirm this was checked. If it
  was not, an ARN model id could still fail with a signature mismatch rather than
  a 404 — that would be a real bug worth an issue, but it needs live Bedrock
  verification, not a docs read.

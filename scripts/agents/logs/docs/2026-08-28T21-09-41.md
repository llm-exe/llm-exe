# docs agent — 2026:08:28T21:09:41

- **Branch**: agent/docs/2026-08-28
- **Started**: 2026-08-28T21:09:41Z
- **Finished**: 2026-08-28T21:16:00Z
- **Status**: complete
- **PR**: https://github.com/llm-exe/llm-exe/pull/773

## Summary

- Checked assigned work (#755, `documentation`). PR #754 was **closed without merging**, so the xAI `effort` fix never landed and `isReasoningModel` still only covers `grok-4.3`/`grok-4.5`/`grok-4.6`. The existing warning callout in `docs/llm/xai.md` is still accurate, so I left it alone and commented on #755 explaining it stays blocked on #732.
- Fixed real drift in `docs/llm/xai.md`: commit `dbdeda4` deprecated six shorthands (`xai.grok-2`, `xai.grok-3`, `xai.grok-3-mini`, `xai.grok-4`, `xai.grok-4-fast`, `xai.grok-4-1-fast`) but the docs listed them as ordinary options and used two in the usage examples. Split into active/deprecated tables with migration guidance.
- Gave `docs/llm/gemini.md` the same treatment — it had no shorthand table at all and used the deprecated `google.gemini-2.5-flash` throughout. Also documented the exact `effort` → `thinkingBudget` value mapping.
- Fixed the landing page (`docs/index.md`) provider-switch snippet, which advertised `xai.grok-2` and `google.gemini-2.0-flash` — both now fail at the provider API.
- Filed #772 (`bug`): Gemini's `effort` allowlist only covers `gemini-2.5-*`, so `effort` is silently dropped on every current `google.gemini-3.*` shorthand. Same class of bug as #732 on the xAI side.

## Files Changed

- `docs/llm/xai.md` — active/deprecated shorthand tables, migration guidance, live models in examples
- `docs/llm/gemini.md` — new shorthand tables, live models in examples, precise `effort` mapping, `#772` warning callout
- `docs/index.md` — landing page provider-switch snippet uses live models
- `docs/public/llms-full.txt` — regenerated via `scripts/generate-llms-txt.sh`

## Next Steps

**Same deprecation audit still owed for the other providers.** Six xAI + six Google shorthands were handled this run; these are still deprecated in source but not marked in docs:

- `openai.gpt-4`, `openai.gpt-4.1-nano`, `openai.gpt-5-mini`, `openai.gpt-5-nano`, `openai.o3`, `openai.o4-mini` — check `docs/llm/openai.md`
- `anthropic.claude-3-opus`, `anthropic.claude-3-5-haiku`, `anthropic.claude-3-5-sonnet`, `anthropic.claude-3-7-sonnet`, `anthropic.claude-opus-4-6` — check `docs/llm/anthropic.md`

Full list: `grep -rn 'deprecateShorthand("' src/llm/config/`.

**Deprecated shorthands still used in `examples/`** (out of scope this run, low time budget — verify the co-located tests still pass before changing):

- `examples/claude.md` → `anthropic.claude-3-5-sonnet`
- `examples/writeTestsFromCode.ts` → `anthropic.claude-3-7-sonnet`
- `examples/helloWorld.test.ts`, `examples/basicTools.test.ts` → `google.gemini-2.5-flash` (test files — coder agent's call, not the docs agent's)

**Two follow-ups blocked on source fixes:**

- When #772 lands, remove the `effort` warning callout from `docs/llm/gemini.md` and update the allowlist in the options table.
- When #732 lands, close out #755 by updating the `effort` row + warning in `docs/llm/xai.md`.

**Structural idea:** `docs/llm/deprecations.md` explains the warning mechanism but does not list what is actually deprecated. A generated table of all deprecated shorthands there — with per-provider pages linking to it — would keep this from drifting again every time a model is retired. Its `openai.o4-mini` example is at least still a genuinely deprecated shorthand.

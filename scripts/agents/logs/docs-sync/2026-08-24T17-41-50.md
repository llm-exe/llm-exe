# docs-sync agent — 2026:08:24T17:41:50

- **Branch**: agent/docs-sync/2026-08-24
- **Started**: 2026-08-24T17:41:50Z
- **Finished**: —
- **Status**: running

## Summary

- Synced docs against commit `0fbea3c` (ci: run typecheck in CI), which added a `npm run typecheck` step gated to Node 24.x in both `tests.yml` and the `tests` job of `agent-review-pr.yml`.
- Updated `TESTS_DEEP_DIVE.md`: whole-picture and matrix diagrams, per-leg step counts (5 steps on 18/20/22, 7 on 24.x), step-by-step sequence diagram, the rationale for why typecheck is load-bearing (ts-jest `isolatedModules` reports no type diagnostics), a new failure mode, and the quick reference card.
- Updated `AGENT_REVIEW_PR_DEEP_DIVE.md`: tests-job DAG now shows the Typecheck step, prose notes the mirror of `tests.yml` plus the intentional omission of coverage upload, and a tests-job step row was added to the quick reference card.
- Corrected stale source line references in `AGENT_REVIEW_PR_DEEP_DIVE.md` (job conditions, review job, tool allowlist, `allowed_bots`, decide job) which had drifted by 5-15 lines.
- Updated the macro docs: `WORKFLOW_ARCHITECTURE.md` section 9.7 steps row plus a new typecheck-rationale row, section 9.4 tests-job row, and the `tests.yml` summary row in `WORKFLOWS_INDEX.md`.

## Files Changed

| Doc updated | Synced against |
|-------------|----------------|
| `.github/docs/TESTS_DEEP_DIVE.md` | `.github/workflows/tests.yml`, `package.json` (script line numbers) |
| `.github/docs/AGENT_REVIEW_PR_DEEP_DIVE.md` | `.github/workflows/agent-review-pr.yml` |
| `.github/docs/WORKFLOW_ARCHITECTURE.md` | both workflows (sections 9.4 and 9.7) |
| `.github/docs/WORKFLOWS_INDEX.md` | `.github/workflows/tests.yml` |

## Next Steps

- `WORKFLOWS_INDEX.md` line 131 encodes a per-doc line count (`agent-review-pr: 626 lines, 12`). That number is now stale after this edit and will drift on every deep-dive change. Either refresh those counts on each run or drop them from the diagram; recommend dropping them since they carry little value and guarantee drift.
- Line-number citations inside deep dives (`lines 138-141`, `line 135`, and so on) are inherently fragile. Several were already wrong before this run. Consider citing step names instead of line numbers in a future pass.
- Not verified this run: whether other deep dives that describe running `npm run typecheck` locally (for example the coder and bot-respond docs) need a note that CI now enforces it too. Worth a quick grep next run.

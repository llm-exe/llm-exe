# docs agent — 2026:08:07T10:59:08

- **Branch**: agent/docs/2026-08-07
- **Started**: 2026-08-07T10:59:08Z
- **Finished**: 2026-08-07T11:06:00Z
- **Status**: complete

## Summary
- No open `documentation`-labeled issues; picked up the model/option drift flagged in the previous run's Next Steps.
- Regenerated `docs/.vitepress/utils/providers.json` via `npm run docs:update-providers` — it was badly stale, so every `<ImportModelNames>` list on the site was missing the Claude 5 family (`opus-5`, `sonnet-5`, `fable-5`, `opus-4-8`), GPT-5.4/5.5/5.6, xAI `grok-4.20`/`grok-4.20-reasoning`/`grok-4.5`, `gemini-3.5-flash`, Ollama `qwen3.5`/`gemma4`/`gpt-oss`, and current deprecation badges.
- Corrected the Anthropic options doc: the sampling-restriction note named only `claude-opus-4-7` (source also rejects sampling params for `opus-5`, `sonnet-5`, `fable-5`, `opus-4-8`), and `effort` was missing from the table entirely — now documented with the adaptive-thinking mapping, the 4.5-generation thinking-budget mapping, and the fact that `opus-5` is deliberately not escalated to `xhigh`.
- Corrected `docs/llm/xai.md`, which claimed `effort` is unsupported by every xAI model; the config passes `reasoning_effort` through for `grok-4.3` and `grok-4.5`.
- Filed [#732](https://github.com/llm-exe/llm-exe/issues/732) (`bug`): `xai.grok-4.20-reasoning` resolves to a model that `isReasoningModel` doesn't match, so `effort` is silently dropped for it.
- Verified `npm run typecheck` passes; verified the deepseek shorthand table and its "effort ignored" note still match source exactly (deepseek sets no `isReasoningModel`).

## Files Changed
- `docs/.vitepress/utils/providers.json` — regenerated from `src/llm/config/*` (68 insertions, 18 deletions).
- `docs/llm/anthropic.options.part.md` — added the `effort` row, corrected the sampling-restriction model list, documented per-model effort mapping.
- `docs/llm/anthropic.md` — examples and frontmatter description now use `claude-sonnet-5`.
- `docs/llm/xai.md` — corrected the `effort` option description.

## Next Steps
- **Automate the providers.json regen.** It drifted by an entire model generation between runs. A CI check that runs `npm run docs:update-providers` and fails on a dirty tree would close this permanently — that's a config change, so it needs the coder agent or the maintainer.
- The xAI and Deepseek docs still carry hand-maintained shorthand tables alongside `<ImportModelNames>`. Both matched source this run, but the duplication is the same drift trap. The tables do add the default-model mapping the component lacks; consider teaching the component to render defaults, then deleting the tables.
- `docs/llm/openai.md` and `docs/llm/gemini.md` were not audited this run — worth checking their option tables against `src/llm/config/openai` and `src/llm/config/google` for the same kind of per-model `effort`/sampling drift found in anthropic and xai.
- Still not done (carried over): verify `docs/examples/**` snippets compile against the current API. `examples/` has real `.test.ts` coverage, but the markdown snippets under `docs/examples/` are not compiled by anything.
- If #732 is fixed, `docs/llm/xai.md`'s `effort` row needs updating to include `grok-4.20-0309-reasoning`.

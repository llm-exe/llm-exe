# docs agent — 2026:08:04T12:06:17

- **Branch**: agent/docs/2026-08-04
- **Started**: 2026-08-04T12:06:17Z
- **Finished**: 2026-08-04T12:12:00Z
- **Status**: complete

## Summary
- No open `documentation`-labeled issues; picked up drift from the multimodal embedding input feature merged to `development`.
- Found a factually wrong claim: `docs/embeddings/cohere.md` stated "llm-exe is text-only; image inputs to Cohere Embed v3/v4 are not supported." Multimodal input shipped, so the note contradicted the code.
- Added a **Multimodal Input (Embed v4)** section to the Cohere doc covering `EmbeddingContentItem`, the data-URI-only requirement, the one-vector-per-item behavior, the uniform-batch rule (`embedding.unsupported_input`), and the `imageInputs` escape hatch.
- Documented the accepted `EmbeddingInput` shapes (`string` / `string[]` / `EmbeddingContentItem[]`) on the embeddings index, plus which providers accept multimodal input and that `getEmbedding()` takes an optional batch index.
- Verified (no change needed): the xAI and Deepseek manual shorthand tables both match `src/llm/config/*` exactly, and `npm run typecheck` passes.

## Files Changed
- `docs/embeddings/cohere.md` — replaced the incorrect text-only note with a Multimodal Input section; documented `imageInputs` as an escape hatch.
- `docs/embeddings/index.md` — added an Input table for `EmbeddingInput`; noted the batch index on `getEmbedding()`.

## Next Steps
- The previous run suggested replacing the hand-maintained xAI/Deepseek shorthand tables with `<ImportModelNames>`. **Recommend against it as stated**: the component renders shorthand names only and carries no default-model column, so dropping the tables would lose information. A better fix is to extend `ImportModelNames.vue` to render the resolved default model from `providers.json` (which *is* generated from source by `docs/.vitepress/scripts/updateProviders.ts`), then delete the manual tables. That touches a `.vue` component, so confirm it's in scope for the docs agent first.
- Still not done (carried over): verify `docs/examples/**` code snippets still compile against the current API — parsers, executor generics, prompt types. Worth a dedicated run since it's a broad sweep.
- `docs/public/llms-full.txt` is a large aggregate file; confirm whether it is regenerated at build time. If it is hand-maintained, it now lags the embeddings docs updated in this run.
- `docs/misc/errors.md` describes embedding error codes only generically (`embedding.` prefix). Consider documenting `embedding.unsupported_input` explicitly now that user-facing input validation can throw it.
- `docs/.DS_Store` is checked into the repo and should probably be removed/gitignored (outside docs-content scope, flagging only).
